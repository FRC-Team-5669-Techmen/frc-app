---
title: "Design system synced to Claude Design: the Core and Brand 15, verified on a real deck"
date: 2026-08-23
branches: []
commits: ["cf75106"]
migrations: []
subsystems: ["Design system"]
record_order: 21
---

 Earlier the same day — Design system SYNCED to Claude Design — the Core + Brand 15 only, scope deliberately held. The first-time sync had stopped short of the upload, blocked on the claude.ai login lacking design-system scopes; that cleared, so the project was created (FRC 5669 Techmen Design System, 0ae827ec-d481-4eb8-a051-772544deb9c4, pinned in .design-sync/config.json) and 83 files uploaded and confirmed by list_files: the bundle + styles + README + _ds_sync.json + _ds_needs_recompile, _preview/*.js x15, _vendor/react{,-dom}.js, and components/** x15 as .jsx/.d.ts/.html/.prompt.md. Cards index off each preview HTML first-line @dsCard marker, so no register_assets was needed. Local-only artifacts were deliberately withheld (.ds-build-meta.json says so in its own emitter comment): .ds-bundle, .render-check.json, .review.html, .stories-map.json, _screenshots/. PREVIEWS ARE COMMITTED, not ignored — .design-sync/previews/*.tsx are build INPUTS, hand-authored, not generated: the package shape has no generated preview tier (generatePreviewSource returns null), the build calls .design-sync/previews/ owned and .cache/previews/ generated, the cache self-defends with its own * .gitignore, and the rm guard refuses to delete anything under .design-sync/. Losing them would downgrade every card to the floor-card fallback and change the renderHashes, forcing a spurious re-upload. VERIFIED ON ONE REAL GENERATED DECK before committing to the ~77-component pass, all four questions answered from live measurements rather than inspection: the preview iframe is cross-origin and its serve URL is token-gated, so a 4th DIAGNOSTIC sheet was added that measures and PRINTS its own computed values. (1) Deck root className measured exactly frc-deck — .frc-deck YES, ground class NO; grounds landed on the section sheets instead. Nothing rendered unstyled because tokens/colors.css splits the scopes: the raw palette sits on :where(.frc-deck, .frc-ground-*) while the 36 semantic aliases sit ONLY on the three ground classes, and the deck-stage script reads --bg0/--edge off the ACTIVE SHEET. Latent risk is narrow — content directly in the deck root, or a sheet missing its ground, gets palette and no aliases. DIAGNOSIS: the TEMPLATE fix, not the :root fix. Moving tokens to :root would undo a deliberate decision (theme.css defines --gold/--fault/--font-mono too) and the scope measured correct; the actual defect is that templates/Deck.dc.html, whose root already reads frc-deck frc-ground-squadron frc-audience-internal frc-letterbox and which _ds_manifest.json registers as the startingPoint, is outside the converter emit set and never uploaded, so the deck was generated from Blank. Reported, NOT changed. (2) --gold resolved #FFE629 on the deck root, the exact published Techmen Gold, no fallback. (3) Ground switching reswatches: squadron accent #FFE629 / bg0 #000000 / glow present, field accent #FFE629 / bg0 #0E1013 / weaker glow, paper accent #7A6300 bronze / bg0 #E9E7E1 inverted / glow none — paper behaves exactly as specified, and it is visible in the deck (sheet 1 near-black, sheet 2 lighter slate). (4) Copy IS editable on the canvas: in Edit mode both StencilTitle and Eyebrow exposed a Text > Content field holding the child string, both were retyped and updated live keeping component styling (gold+glow, gold dash + mono caps), then discarded. So the copy-placement rule in FRC_Design_System.md holds and the 26 sheet patterns are not built on a false assumption. --ink printed empty in all three grounds, which is correct rather than a miss: this DS has no --ink token, foreground is --fg/--fg-dim/--fg-hero. Data, Surfaces, Forms and Sheets remain unsynced by instruction.
---
title: "Design system pass 3: the twenty-six sheet patterns, the specimen, SKILL.md"
date: 2026-08-22
branches: []
commits: ["685c70c"]
migrations: []
subsystems: ["Design system"]
record_order: 25
---

 Earlier the same day — Design system PASS 3 — components/sheets/, the twenty-six full-sheet patterns, plus templates/Specimen.dc.html, SKILL.md and a rewritten README as the full brand guide. Registry is now 77 components / 6 demo cards and NOTHING in the spec's manifest is pending; only the asset files are outstanding. NEW tokens/sheets.css (layout only, appended to the fixed import order) and an internal components/sheets/Sheet.jsx frame that applies the transition, stacks ambient layers and renders the footer rail. THE LOAD-BEARING RULE: a pattern inherits ground and audience from the deck. tokens/sheets.css contains no ground selector at all and no pattern names one — ds-audit now FAILS on a ground class or an audience prop in any sheet file, on a sheet defaulting to a fifth transition, on a sheets group that is not exactly 26 patterns, and on the Specimen ever being listed as a startingPoint. MatchBreakdownSheet is the fourth and last legal place for --alliance-red/--alliance-blue; its scoping was added to tokens/data.css beside the other three precisely so sheets.css could stay ground-free, and the /_ds alliance scanner's allowed list was widened from three names to the spec's four. HubSheet is the one sheet with footer={false} and therefore the one sheet with no FIRST mark — the rail is what carries 5669. SafetySheet THROWS without a SafetyNote. VERIFIED IN THE BROWSER at /_ds (dev server 5175, --mode dsspec) by stepping all 26 patterns through all THREE grounds x BOTH audience modes = 156 pattern renders, every combination green: 0 hidden-content offenders across 1208 text-bearing elements (27 audience-chrome switches counted separately, never excused silently), 0 stage overflow, 25 of 26 sheets carry a FIRST mark and all 25 carry 5669 (HubSheet is the 26th, correctly markless), 361 slotted copy elements holding 6831 characters, transitions measured 10 shutter / 9 boot / 6 banner / 1 cut, 0 gold on paper across 1555 elements, and with ambient layers switched ON 26 layers all clipped inset(0px 0px 96px) and 0 intersecting the FIRST logo zone on any ground. Alliance: 62 legal declarations on FIELD across AllianceSplit / ScoutRow / MatchBreakdownSheet / FieldDiagram, 0 leaks, 0 on squadron and paper. TWO measurement bugs were found and fixed rather than worked around: (1) the hidden-content detector flagged .frc-footer-first on every internal sheet — that zone is audience chrome the external mode adds, so it is now counted as a chrome switch and reported, not excused; (2) the overflow scan flagged exactly the 9 boot sheets by ±9px because this browser pane does not composite, so CSS animations sit at frame 0 and frc-slide-boot's from-frame is scale(1.015) blur(16px) opacity(0) — proofs now measure inside withStaticTransitions(), which suspends the sheet transitions and measures the GUARANTEED BASE STATE, which is the rule the motion library actually promises. templates/Specimen.dc.html validated structurally: 26 sections, all 26 patterns once, 9 squadron / 9 field / 8 paper, 4 external / 22 internal, transitions 10/9/6/1 matching the React defaults, HubSheet the only footer-less sheet. It is marked REFERENCE ONLY in the file, in its prompt.md and in the manifest (copied: false). ds:audit green at 275 files; production build clean and the DS still absent from dist.
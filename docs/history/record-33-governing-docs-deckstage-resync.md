---
title: "Governing docs committed, DeckStage reconciled against them, resynced to 16, check 43 measured"
date: 2026-08-23
branches: []
commits: ["c62eda1", "646d654"]
migrations: []
subsystems: ["Design system"]
record_order: 18
---

 Earlier the same day - Governing docs committed, DeckStage reconciled against them, resynced to 16, check 43 measured PASS on a generated deck. docs/FRC_Design_System.md v1.6 and docs/FRC_CLAUDE_DESIGN_STANDARDS.md v1.5 are now IN THE REPO at src/lib/design-system/docs/, referenced from README.md, SKILL.md and CLAUDE.md — a prior session built DeckStage without them because an attachment failed to arrive, and a repo file at HEAD cannot fail to arrive. RECONCILIATION FOUND ONE REAL GAP, NOT A DOC NIT: .frc-letterbox and .frc-thumbs-dock had their CSS (position:fixed/inset:0/overflow:hidden on the letterbox, position:absolute on its child .frc-stage) declared ONLY inside the two templates own inline style blocks, never in tokens/deck-motion.css or anywhere shipped in styles.css. Since nothing is copied any more, a deck assembled from Blank that set .frc-letterbox per the routing header got a class DeckStage read correctly but that did NOTHING visually — no fixed positioning, no clipping, no absolute stage to scale into. FIXED by moving both rules into tokens/deck-motion.css beside .frc-stage/.frc-thumbs, and stripping the now-redundant duplicates from both templates (whose headers already claimed nothing below is a token, which is now true instead of contradicted). VERIFIED in headless Chrome against the bundled styles.css closure: a .frc-deck.frc-letterbox root resolves position fixed/inset 0/overflow hidden, its child .frc-stage resolves position absolute/top 0/left 0. NO OTHER DISAGREEMENT: DeckStage was already correct against check 43 and v1.6 before the docs could be read — it does NOT guard on missing .frc-letterbox (deliberate, matches both documents verbatim: the one thing DeckStage cannot guard on because its absence is legal for an embedded deck), and its six guard states are a superset of, not in conflict with, the four v1.6 summary sentence names. Cosmetic: DeckStage moved to sit right after DeckFooter in both _ds_manifest.json and brand/index.js, matching the doc brand-group order — no functional effect. RESYNC: DeckStage added to componentSrcMap/docsMap, .design-sync/previews/DeckStage.tsx authored (Squadron + Paper variants, the pair that proves painting-from-the-sheet not the root since --edge and --bg0 differ visibly). Rebuilt 16/16 components, 16/16 previews render cleanly; brand__DeckStage.png shows a black squadron cell and a light paper cell side by side. Uploaded 88 files (83 prior + 5 new). Manifest did not recompile from the upload alone — same as first sync — needed the project page opened once in a browser to run the platform self-check; after that _ds_manifest.json reports 16 components including DeckStage, 16 cards. DECK GENERATED AND CHECK 43 MEASURED: routing header written per the standards v1.5 skeleton, with one addition flagged — the doc own routing-header block lists only FOUR explicit set-on-root bullets (aspect/ground/audience/mount DeckStage), with .frc-letterbox mentioned only in prose under the DeckStage bullet, not as its own bulleted class, even though check 43 treats it as a fifth independent required item; the prompt used here added an explicit fifth Letterbox class bullet to satisfy check 43 unambiguously, and this is flagged as a loose end worth tightening in the standard itself, not a defect in DeckStage or the audit. MEASURED, not assumed: first against the raw .dc.html source via GetFile, then against LIVE computed values from a temporary diagnostic sheet mounted in the actual generated deck. All six raw values, verbatim: deck root className = frc-deck frc-ground-squadron frc-audience-internal frc-letterbox; .frc-stage computed width = 1920px; height = 1440px; data-aspect = 4:3; .frc-letterbox === deck root -> true; count of [data-frc=DeckStage] = 1. ALL FIVE CHECK-43 ITEMS PRESENT: aspect 4:3 1920x1440 not the 16:9 default, a ground class, an audience class, .frc-letterbox (correctly IS the root, not a wrapper), DeckStage mounted exactly once (exactly one x-import element in source, live count 1). This clears the last blocker recorded against syncing the remaining 62 components. Data, Surfaces, Forms and Sheets remain unsynced by instruction.
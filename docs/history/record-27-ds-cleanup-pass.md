---
title: "Design system cleanup pass against spec v1.2: guards, harness, marks, capture"
date: 2026-08-22
branches: []
commits: ["fa3d409"]
migrations: []
subsystems: ["Design system"]
record_order: 24
---

 Earlier the same day — Design system CLEANUP PASS against FRC_Design_System.md v1.2 and FRC_CLAUDE_DESIGN_STANDARDS.md v1.2 — see the pass-4 sentence in the Built-so-far bullet. Seven items. (1) DeckFooter now defaults to mark="logotype"; both templates' rails were switched from the seal slot to the auto logotype slot, and CoverSheet/ClosingSheet keep the seal in the sheet BODY, which is where the spec wants it. (2) NEW components/guard.jsx: every guard renders a rust fault marker at run time and throws only when setHarnessMode(true) — /_ds turns it on, a capture run turns it off so the image of a tripped guard can be captured. FirstName's audience-dependent guard is gone; `audience` on FirstNameScope is now record-only. The old .frc-first-fault pill was deleted in favour of the one shared .frc-fault marker. (3) Harness reproducibility: .env.dsspec and .claude/launch.json are now TRACKED (.gitignore gained a !.claude/launch.json exception), npm run ds:dev boots it, and App.jsx gained `onSpecimenPath` which lets ONLY /_ds past the splash/AccessGate/application gates and only where SpecimenPage exists (dev). (4) scanForAlliance is program-aware: it resolves the element's own --program and classifies a match as a collision, so the FLL-red/alliance-red overlap is reported as a documented finding instead of a false-positive leak. (5) The three team marks were fetched from https://frcteam5669.com/assets/logos/marks/svg/ and compared byte-for-byte before wiring. FINDING: public/assets/logos/Mark-Gold.svg in this repo is RECOLORED — #D4AF37 where the canonical file carries #ffe629 — so the canonical files were wired into src/lib/design-system/assets/team/ and the app copy was left untouched (it is an app surface outside this bundle; wiring it is a separate call). White and Black matched apart from CRLF. assets/PROVENANCE.json pins each sha256 and ds:audit re-checks them. (6+7) NEW scripts/design-system/ds-capture.mjs (npm run ds:capture) + a capture mode on /_ds (?capture=Pattern&ground=&audience=), driving the machine's Chrome through playwright-core (a devDependency; no browser download). VERIFIED: 156 PNGs written to artifacts/ds-capture/<ground>/<audience>/, 0 guard fault markers and 0 page errors; a reduced-motion run with prefers-reduced-motion actually SET writes artifacts/ds-capture-reduced-motion/; and /_ds was booted from a FRESH CLONE with no local env. The capture found what three passes of DOM measurement never could: StencilTitle's bridge rendered as a STRIKETHROUGH across every hero title on every ground, so `bridge` now defaults to off (it stays available for a single struck word). That is the whole argument for check 41.
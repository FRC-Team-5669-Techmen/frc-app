---
title: "Fail-safe default ground, and templates/ confirmed unshippable"
date: 2026-08-23
branches: []
commits: ["cc9498e"]
migrations: []
subsystems: ["Design system"]
record_order: 20
---

 Earlier the same day — Fail-safe default ground, and templates/ confirmed unshippable. (1) tokens/colors.css keeps its split — raw palette on :where(.frc-deck, .frc-ground-*), the 36 semantic aliases on the ground classes — but .frc-deck now SHARES the SQUADRON literal block instead of resolving nothing. One declaration, not a duplicated set. The bug closed is PARTIAL resolution, which is worse than total failure: a groundless root still painted --gold from the palette so it looked almost right. Source order is load-bearing and is now asserted, because an element carrying .frc-deck AND a ground class ties on specificity and the later block wins. MEASURED in headless Chrome against tokens.js (not a hand-typed list): six states — content directly in a groundless .frc-deck root, a .frc-sheet with no ground class, a field sheet, a paper sheet, one element carrying .frc-deck AND .frc-ground-paper, and an element nested deep under paper — all resolve 36/36, 0 mismatches. POSITIVE CONTROL: with the .frc-deck line removed the first two resolve 0/36 while --gold still resolves #FFE629, reproducing the almost-right state exactly. NEW ds-audit check 14 in four parts: .frc-deck shares the squadron block and nothing duplicates the 36; field and paper stay after it in source order; each of the four reachable states resolves the full set, with the deck-root state DERIVED from the real selector rather than assumed; and every fallback-less var() in the token sheets is declared somewhere (declarations gathered across all token sheets, since typography declares the scale surfaces.css consumes). Four negative controls all caught, and the clean tree still passes. (2) templates/ CANNOT be emitted — reported, not forced. The converter has no template concept at all: the entire cfg.* surface was enumerated and template/startingPoint appear nowhere in build, validate or resync. The platform DOES support templates, shape learned from the IDEA design system in the same account whose four work — templates/<slug>/ holding entry + scripts + images + a captured .thumbnail, registered in the platform-compiled _ds_manifest.json under templates[]. CRUCIALLY the platform reads templates, NOT startingPoints, which is empty even in IDEA — so our own manifest startingPoints key is local vocabulary matching nothing upstream, which is the quiet part of the gap. HAND-UPLOAD TESTED and it does not register: templates/deck/Deck.dc.html landed, the _ds_needs_recompile fence was CONSUMED so the self-check demonstrably ran, and the recompiled manifest still said templates: [], startingPoints: [], hasThumbnailHtml: false; no entry in CHOOSE A TEMPLATE and no Templates nav section (IDEA has one). Probe deleted afterwards. A working folder would also need a .thumbnail, a root thumbnail.html, and a vendored copy of the platform-generated support.js dc-runtime marked do-not-edit — a real decision, so not taken. Docs corrected instead: a templatesNotEmitted block in _ds_manifest.json, plus the Deck.dc.html header and both README claims now say the shell is a hand-copy artifact Claude Design never sees. WHAT THE BLANK PATH LOSES, read from the generated deck source through the app GetFile RPC: 4:3 1920x1440 ABSENT (data-aspect 16:9, no 1440 anywhere), letterbox ABSENT (the string never occurs), deck-stage script ABSENT as written and only partly re-created (frcDeckStage and getComputedStyle(sheets[current]) both absent; the deck rolls its own keydown/resize/scale and reads --bg0 but never --edge, so painting canvas and letterbox from the active ground is gone), audience class ABSENT, thumbnail rail ABSENT — but the FOOTER RAIL IS PRESENT, because DeckFooter is a component rather than template markup, which is why the rail, the 5669, the deck name and the sheet counter all rendered. No further component groups synced: Data, Surfaces, Forms and Sheets stay unsynced by instruction.
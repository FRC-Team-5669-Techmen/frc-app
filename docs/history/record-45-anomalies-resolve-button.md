---
title: "Capped and double-in anomalies gain a Resolve button into MemberHoursAdmin"
date: 2026-08-30
branches: ["claude/anomaly-resolve-workflow-zq8e30"]
commits: ["33a5f9a"]
migrations: []
subsystems: ["Hours and attendance"]
record_order: 6
---

 Earlier the same day - Attendance anomalies became actionable: `capped` and `double_in` cards on `/verify-hours` gained a Resolve button that deep-links into MemberHoursAdmin scoped to the member and the flagged event row. See the Anomaly-flags sentence in the accountability bullet. THE ONE HARD CONSTRAINT is that the missing-check-out field is EMPTY, always: the whole point of a Resolve button is to remove the re-finding, not the judgement, and an hours ledger that offers a plausible default gets a wrong value saved fast. Verified by grep over the diff - the only `value=` the change adds anywhere is `value={outTime}`, whose state is `useState('')` written only by the user typing or by the post-save reset, and no `defaultValue` is introduced; the pre-existing `${todayStr()}T18:00` default on the unrelated `+ Add event` form was left alone. TWO PROOFS RUN, both with negative controls. (1) A pairing test that extracts the SHIPPED `unclosedInIds` out of MemberHoursAdmin.jsx by regex and evals it against the real `detectAnomalies` across 7 ledgers: 23/23, and removing the orphan-IN branch fails 3 of them. It found the reason the function must exist - `sessionsFromEvents` cannot answer 'which INs are unclosed' because it overwrites an open IN on the next IN, so a double_in orphan produces no session row to read. (2) A headless-Chromium drive of the REAL VerifyHoursPage + MemberHoursAdmin against a stubbed `./supabase` (vite alias, no creds, no network, harness deleted after): 32/32, including value === '', the DOM `value` attribute absent, the input IS document.activeElement, the two sibling rows neither highlighted nor auto-opened, and the highlight class gone after ~2s while the field stays empty. LOOKED AT, not only measured: the screenshot shows the 08:00 orphan IN highlighted gold and open with `mm/dd/yyyy, --:-- --` and the mm segment focused. NOT DONE, deliberately: detectAnomalies is untouched, overlap/geofence get no button, no default-end-time-by-weekday feature, and no new table, RPC or RLS policy - the fix path is the existing `staff_add_event`/`staff_set_event`/`staff_void_event` SECURITY DEFINER RPCs. TWO ITEMS IN THE BRIEF DO NOT EXIST IN THIS REPO and were reported rather than invented: there is no `svelte-check` (this is React + Vite; `npm run build` is the build gate and passes) and no `tools/browser-verify/routes/` harness anywhere in the tree. Live-flow verification against a real anomaly and a real member is still Mr. Pina's step on the Vercel preview - it needs a signed-in Bosco Tech / team account.
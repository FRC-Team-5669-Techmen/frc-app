---
title: "Design system pass 2: data, surfaces and forms component groups"
date: 2026-08-22
branches: []
commits: ["c1ad90d"]
migrations: []
subsystems: ["Design system"]
record_order: 26
---

 Earlier — Design system PASS 2 — the data, surfaces and forms component groups at src/lib/design-system/, 36 new components (16 data / 18 surfaces / 2 forms) each with .jsx/.d.ts/.prompt.md, one demo card per group (DataDemoCard / SurfacesDemoCard / FormsDemoCard), three NEW token sheets (tokens/data.css, tokens/surface-components.css, tokens/forms.css) appended to the fixed styles.css import order, and components/slots.jsx. Registry is now 51 components / 5 cards; pending is the sheets group and templates/Specimen.dc.html only. ONE new ground alias: --surface-viewport (36th), declared as a literal in all three scopes, mirrored in tokens.js and the manifest, and added to PAPER_MUST_DIFFER — it is the ImageFrame backplate, so a ground retints it and no component hardcodes it. Enforced in code, not in prose, and proven in the browser: ImageFrame bleed THROWS on kind="screenshot"; Cutout THROWS on fit="cover"; SponsorTier THROWS on any mark that is not a Cutout ground="none". tokens/image-slot.css now overrides image-slot::part(frame) to kill the platform's rgba(127,127,127,.08) wash — in the stylesheet, never in a copied image-slot.js. Alliance containment: --alliance-red/--alliance-blue are set as local vars only under .frc-ground-field and only by AllianceSplit, ScoutTable and FieldDiagram; MatchClock uses --fault (rust) at zero, never alliance red. SubteamBadge imports src/subteams.js (the DS reaches OUT to the app for that one vocabulary rather than copying it — deliberate, since a copy is what CLAUDE.md forbids). ds-audit gained the new import order and now scans components/(core|brand|data|surfaces|forms) for unlisted .jsx files. VERIFIED IN THE BROWSER at /_ds (dev server on 5175, --mode dsspec): 108 computed alias values across 36 aliases x 3 grounds all resolve to their own ground literal (0 mismatched, 0 frozen, 0 gold on paper); ImageFrame backplate measured equal to --surface-viewport on all three grounds (#141619 / #1E222A / #DCD9D1); bleed measured 0px on all four edges with the bracket pseudo display:none while the render variant still draws its brackets; the empty slot measured a dashed 1px hairline on a transparent ground; image-slot::part(frame) measured transparent against a route-only stub that paints the platform wash (the stub is in the specimen, not the bundle, so the override is measured rather than asserted); a real runtime-generated transparent PNG in Cutout produced 0 rectangle offenders on all three grounds x all three cutout grounds with object-fit contain and drop-shadow-only grading (the shelf datum line is reported separately as a <=4px rule, not a plate); MatchClock rendered 0:20 and 2:20 at rest in hero gold, copper rgb(217,140,63) in the warning window and rust rgb(176,80,60) at zero; a page-wide scan of 2814 deck elements found 36 legal alliance-colored declarations (all inside AllianceSplit/ScoutTable/FieldDiagram on FIELD), 0 leaks, and 1 documented VALUE COLLISION — FIRST LEGO League red is published as the same hex as alliance red, so a computed style cannot tell program chrome apart from alliance data after substitution, and the proof reports that rather than hiding it; the three refusals each threw their real message into an error boundary; 707 elements across the three new demo cards on paper rendered 0 gold; 64 component roots mounted from source; 0 zero-sized or overflowing roots in the new cards. Production build clean and the DS is still absent from dist (grep for frc- in dist/assets returns 0).
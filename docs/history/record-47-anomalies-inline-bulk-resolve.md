---
title: "Attendance anomalies resolvable inline and in bulk on /verify-hours; src/hoursResolve.js"
date: 2026-08-30
branches: []
commits: ["af612d3"]
migrations: []
subsystems: ["Hours and attendance"]
record_order: 4
---

 Earlier the same day - Attendance anomalies became resolvable INLINE and IN BULK on /verify-hours, and the duration math moved into a new shared pure module src/hoursResolve.js. See the RESOLVING NOW HAPPENS sentence in the accountability bullet. THE PROMPT'S CLAIM ABOUT eventIds WAS VERIFIED BEFORE ANYTHING WAS BUILT ON IT, by reading detectAnomalies, sessionsFromEvents and cappedSession rather than trusting the brief: it holds exactly - a closed pair always yields outId so capped is length 2, a trailing open IN yields outId null so capped is length 1, and double_in is always [orphanIn, nextIn]. That split is the whole routing rule, so it had to be read. THE DESIGN DECISION WORTH KEEPING: hoursResolve models the value as an OFFSET IN MINUTES where null means nothing entered, which makes the no-pre-filled-timestamp rule structural rather than a habit - there is no zero-ish instant to render by accident, and exactly one function turns an offset into a timestamp. Both duration states initialise `useState(null)` and expandCard resets to null on every expand, so no card can inherit another's number; the only `value=` props the diff adds anywhere are `value={reason}`, which is text. TWO REAL DEFECTS WERE FOUND BY RUNNING IT rather than by reading it, both fixed and both in the app: (1) the bulk result line lived inside the bulk bar, and a successful bulk action empties the selection and unmounts that bar, so `2 resolved.` was destroyed at the instant it became true - the note and the progress counter now render outside it; (2) marking a selected preset with `mha-btn-go` overloaded the class that means PRIMARY ACTION everywhere else in that panel, making the lit preset and the Add check-out button indistinguishable to a selector and nearly so to an eye - selection now has its own filled-gold `.mha-btn-on`. VERIFIED IN A BROWSER twice over, against a stubbed ./supabase (vite alias, no creds, no network, both harnesses deleted after): 62/62 on a four-anomaly ledger covering the inline/heavy split, a single inline resolve end to end, the reason carrying over while the duration does NOT, the two-click void, bulk void of 2, bulk duration-set of 2 proved per-item by the two instants being 4h apart exactly as their check-ins are, and a stubbed partial failure reporting `1 resolved, 1 failed` with the failed card still present carrying its own error; plus 25/25 re-running last session's per-member checks so the EventRow refactor onto the shared module is proved unchanged rather than assumed. 0 page errors on both. LOOKED AT, not only measured: screenshots of the full list and the expanded card. SEQUENTIAL, NOT Promise.all, and the reason is written at the function. NOT DONE, deliberately: no batched RPC, no change to detectAnomalies/unclosedInIds/any RPC signature, no persistence of the last reason, and capped-length-2 still routes to MemberHoursAdmin untouched. Live-flow verification against a real backlog is still Mr. Pina's step.
---
title: "Weekly survey: member form at /survey, mentor authoring and results at /surveys"
date: 2026-08-30
branches: ["claude/weekly-survey-feature-ior6p1"]
commits: ["900baf6"]
migrations: ["weekly_surveys.sql"]
subsystems: ["Surveys"]
record_order: 2
---

 Earlier the same day -- Weekly survey — a recurring pulse check at /survey with a mentor authoring-and-results page at /surveys, four new tables in supabase/weekly_surveys.sql (NOT APPLIED — run it in the SQL editor). See the new bullet in Built so far. THE DECISION THAT SHAPED THE REST: at most one survey may be open, and it is a partial unique index on ((true)) where is_open rather than client logic or a trigger — which then forces the admin's Open button to be close-then-open as two visible writes, and makes the member surface's "resolve the single open survey" a lookup instead of a tiebreak. THE OTHER ONE: there is no update and no delete policy on responses or answers for anyone, staff included, so editing a submission is NOT OFFERED rather than half-offered; the brief allowed a server-side resubmit-that-replaces and it was deliberately not built, because that needs a SECURITY DEFINER RPC and a form that silently replaces an answer is worse than one that says it cannot. VERIFIED BY RUNNING IT, not asserted: 54 checks in headless Chromium driving the REAL SurveyPage and SurveysAdmin against a stubbed ./supabase (vite alias, no creds, no network, harness deleted after) — full member submission end to end proved at the STORE level (3 answer rows from 5 questions, the skipped multi and the skipped text writing NO ROW rather than "" — the stub raises 23514 on an empty value so this cannot pass by permissiveness), the second visit landing on the already-submitted card with 0 question cards rendered, a forced 23505 race resolving to done with no raw database error shown to the student, the seeded survey with its options TODO still submittable, both nothing-open states, the mentor aggregate against a known fixture (Mechanical 2/50%, Electrical 1/25%, Programming 0/0%, CAD 1/25%, and a multi whose denominator is 3 not 4 because one member skipped it), attribution off by default and naming people when toggled, and authoring/reorder/delete/open/close. A POSITIVE CONTROL runs the SAME comparison function with an expectation wrong by one and asserts it reports the mismatch, so a green aggregate means something. THREE REAL DEFECTS WERE FOUND BY RUNNING AND LOOKING, not by reading. (1) The seed's opens_at was 2026-08-31 and the migration was written on the 30th, so the member surface correctly showed "no survey open" for a survey seeded as open — the honest fix is opens_at null, and the reason is recorded at the seed. (2) FOUND BY LOOKING AT A SCREENSHOT, invisible to every measurement taken up to that point: the sticky submit bar faded from transparent, which put the answered counter in the translucent band painted over a textarea border. The plate is now opaque and edge-to-edge with the fade moved to a ::before strip ABOVE it. The check that pins it compares PIXELS — the footer band captured as-is versus with every question hidden — after a FIRST ATTEMPT MEASURED THE WRONG THING and is recorded as such: elementsFromPoint returns the whole hit-test stack regardless of what is painted on top, so it reported a bleed wherever a card merely sat behind an opaque plate. The pixel check was then NEGATIVE-CONTROLLED by restoring the gradient, which fails it, and restoring the fix, which passes. (3) FOUND BY LOOKING AT THE ADMIN SCREENSHOT: the survey Close button was painted --fault red, but closing a survey destroys nothing and is not an error — red is reserved for faults and destructive actions, so Close is now the neutral button and only the question Delete is red. TWO HARNESS BUGS were found by running it and neither was in the app: an expectation that indexed the results bars by count order when they render in the mentor's AUTHORED order, and a const named `open` shadowing the page-opening helper. NOT DONE, deliberately: no edit-after-submit, no results CSV, no reminder push, no per-season scoping, and the seeded questions 1 and 2 still carry their options as a TODO because the values are not decided. UNVERIFIED, and it is Mr. Pina's step: nothing ran against the live Supabase project — no credentials exist here — so supabase/weekly_surveys.sql MUST BE RUN IN THE SQL EDITOR before /survey works, and the RLS policies were reasoned and written, never executed against a real Postgres.
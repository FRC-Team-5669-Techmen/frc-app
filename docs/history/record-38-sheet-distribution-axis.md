---
title: "Sheet content distribution axis landed and measured"
date: 2026-08-24
branches: []
commits: ["c16a373"]
migrations: []
subsystems: ["Design system"]
record_order: 13
---

 Earlier — Sheet content distribution axis landed — the ~5-file fill change scoped in the previous pass, built exactly as scoped and measured before and after in a live browser. See the distribution sentence in the design-system bullet for the assignment. CAUSE, restated because it is the whole point: .frc-sheet-body ALREADY handed .frc-sheet-content the leftover height as a 1fr row; align-content: start then threw it away for every kind, so the height was received and discarded. THE HEADLINE MEASUREMENT, a GallerySheet holding four filled compact RoleCards on a 1920x1440 stage, taken from the real route with the two CSS files reverted and then restored rather than simulated: BEFORE 583px reached of a 1200px body box = 48.6% fill, 617px of dead ground — which is the ~600px complaint, measured. AFTER 1200px of 1200px = 100% fill, 0px dead. The canonical four-Sample gallery went 46.6% -> 100% on the same stage. Across all 26 mounted patterns: gallery, roster, subteam and hub reached 100%, timeline 97.3%, targets 91.1%, split 69.1 -> 84.3% and sponsor 61.1 -> 78.9% (both by centering, which moves content down rather than growing it — stated because the metric measures REACH, not coverage, and conflating the two would flatter center). TWO STRETCH KINDS FALL SHORT AND ARE REPORTED RATHER THAN FORCED: DataSheet 55.5% and FieldSheet 69.5%. Probed both rather than guessing — FieldSheet's content row is 1019px, .frc-field-main fills it at 1019px and BOTH its columns measure 1019px, so the axis reaches all the way and what stops the fill is FieldDiagram's own fixed aspect; DataSheet's main block correctly fills its row at 836px (1019 minus the stats band and the gap) and the chart inside does not grow. Both causes are inside components this pass was told not to touch. The nine start kinds are unchanged by design and their numbers (comparison 44.2%, blocker 41.2%, schedule 48.1%) are the honest look of a short list, not a defect. ONE REAL ORDERING BUG WAS FOUND BY MEASURING AND FIXED, NOT WORKED AROUND: the align-items relaxation started at the top of sheets.css and .frc-data-main[data-aside] — class plus attribute — TIES it on specificity, so source order decided and DataSheet was assigned stretch while not stretching. The rule now sits at the foot of the file with the reason written at it. A SECOND BUG WAS FOUND IN THE NEW PROOF ITSELF: the verdict counted the five no-content-row kinds as unassigned and read FAIL on a correct tree; align === null now means 'renders no content row', which is a different state from 'assigned nothing' — that distinction is also what check 28 enforces on the source side, where such a kind must instead own its own .frc-sheet-<kind> .frc-sheet-body rule. VERIFIED: ds:audit green at 78 components / 289 files, production build clean, DS still absent from dist, /_ds sheet-fill verdict PASS with 4 fault markers (the documented DeckStage baseline) and one console line, ERR_CONNECTION_REFUSED from the placeholder Supabase URL, which .env.dsspec documents as expected. ds:audit:controls grew 7 negative controls for check 28 (38 total). NOT DONE, deliberately and per instruction: ImageFrame untouched, DeckSteps not built, no pattern .jsx changed. NO SCREENSHOT: the Browser pane would not composite frames in this session, so every number above is a computed-value measurement and none of it is a claim about how it looks.
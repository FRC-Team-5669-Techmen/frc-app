# CLAUDE.md - Techmen Team Platform

Operating rules for working in this repository. Everything here changes how a task is done, whatever the task is. This file is read automatically by Claude Code on startup, so per-task prompts can stay lean. Keep it current (see Self-Maintenance at the bottom).

**The per-bundle historical record lives in [`docs/history/`](docs/history/), ONE FILE PER ENTRY.** Until 2026-09-02 it lived on a single 109,839-character line of this file — the "Last reviewed:" paragraph — which every session rewrote, so two sessions working in parallel always collided on it and nobody could find anything in it. It is now one file per bundle, byte for byte, and `npm run history:verify` proves the split lossless on every CI run. Read it when diagnosing something in a subsystem with history, or when you need the REASON a rule below exists before changing it. **`grep -r` over that directory is how you find an entry**; every file's front matter carries its date, branch, commits, SQL files and subsystem as plain text. `npm run history:index` prints the indexes, which are GENERATED and never committed. Do not read the directory end to end.

Two more directories carry things this file used to swallow:

- **[`docs/decisions/`](docs/decisions/)** — open questions that need Mr. Pina, each with a stated default so a session proceeds rather than blocking.
- **[`docs/prompt-ledger/`](docs/prompt-ledger/)** — every Claude Code prompt recorded when it is ISSUED, so a chat can tell whether work is already in flight before writing a rival prompt for it.

---

## What this repo is

The internal team platform for FRC Team 5669 "Techmen" (Don Bosco Technical Institute). A PWA that is the single hub for everything the team runs on: attendance, hours, member profiles, skills/certifications, and a growing set of feature modules. Attendance was the first module, not the point of the app. The long-term vision is one connected platform for the whole team.

- **Stack:** Vite + React + Supabase + Vercel
- **Live:** frc-app-liard.vercel.app
- **Repo:** https://github.com/FRC-Team-5669-Techmen/frc-app

### Branding

Techmen. "Tactical HUD" theme — near-black base, gold targeting accent, silver/steel chrome. Display/UI face is Chakra Petch; all data/readout lines (timestamps, codes, counts, status) use Share Tech Mono. Both fonts are self-hosted via @fontsource (no Google Fonts CDN). Team logo + "TECHMEN·5669" wordmark in the header, not "5669" or "FRC App". Student-facing name is always Techmen.

**Theme tokens live in `src/theme.css`** — a single source of truth (CSS custom properties) imported once in `main.jsx` before `App.css`. Never scatter raw hex through components; use the vars (`--bg`, `--surface`, `--border`/`--border-strong`, `--text`, `--steel`, `--muted`, `--gold`/`--gold-bright`/`--gold-dim`, `--fault`, `--font-ui`, `--font-mono`, `--radius`). **`--gold` is `#FFE629`, the published Techmen Gold** — it was `#D4AF37`, a generic web metallic gold that is not a team brand color, and the whole ramp (`--gold-bright` `#FFF06B`, `--gold-dim` `#C7B320`, `--gold-a08/a14/a24`, `--hr-regular`, `--hr-build`) is derived from it. **Gold is identity, hero type and active state, never a surface fill.** At this brightness a large gold field out-shouts the team mark on the same screen, so the large primary actions (landing CTA, access-gate submit, login submit, parent-response submit, member-application submit) are a gold label and gold border on the dark plate, with the gold FILL kept for hover and focus. **`.gate-signout` is steel** — steel border, ash label, `--surface` lift on hover — because a sign-out is neither identity nor an active state, and equal weight with the submit was wrong even as an outline. Its keyboard `:focus-visible` ring is deliberately NOT overridden: the ring is the system indicating keyboard position, not the control's identity, so a per-control ring color makes the indicator less reliable to follow, and focus is itself an active state, which is what gold is for. Every focusable control on the gate paints the same `solid 2px rgb(255,230,41)` ring. Small controls — dots, chips, tabs, progress bars, per-row buttons — stay filled, because small-area gold IS the active state. Red (`--fault`) is reserved for genuine faults/errors/destructive actions only. **Role colors** are tokens too (`--role-parent/-student/-mentor/-lead/-admin`, same pattern as `--hr-*`); render roles via the shared `RoleBadge`/`roleColor`/`topRoleOf` from `src/roles.jsx` (styled by the global `.role-badge`/`.role-dot` primitives in theme.css) so the same role is the same color on the roster, access requests, coverage matrix, nav avatar menu, and profile.

### Tech stack

- Vite + React. PWA via vite-plugin-pwa in **`injectManifest`** mode — custom service worker at `src/sw.js` (precache + autoUpdate + web push). Not `generateSW`.
- Fonts: Chakra Petch + Share Tech Mono, bundled offline via @fontsource (woff2 precached by the service worker). No runtime font CDN. (The one exception is the deck design system under `src/lib/design-system/`, which loads Space Grotesk / Space Mono / Roboto from Google Fonts by spec; nothing in the app shell uses it.)
- Supabase: Postgres + auth + RLS. This is live team data.
- Deployed on Vercel, auto-deploys on push.
- Auth: email OTP (6-digit code) and Google OAuth via an Internal consent screen scoped to boscotech.edu. Every student has a school Google account.

### The subsystems

- **Design system** (`src/lib/design-system/`, namespace `FRC5669DesignSystem`, class prefix `frc-`). The presentation/materials identity system specified by **`docs/FRC_Design_System.md`** (brand, tokens, type, motion, the component manifest) and **`docs/FRC_CLAUDE_DESIGN_STANDARDS.md`** (scoping and prompting — recipes, the routing header, the pre-delivery audit) — both committed under `src/lib/design-system/docs/` so a session reads them from HEAD rather than depending on an attachment arriving; project knowledge holds the copy Alejandro maintains, and a version ships to both in the same pass. Authored as React so Claude Design can source it from GitHub. **Built fresh from the spec, NEVER extracted from the app's CSS or components** — the app predates any token layer, and the two share NO stylesheet: every DS token is scoped to `.frc-deck` and the three ground classes (never `:root`), so loading the DS cannot collide with `theme.css`. **Migrating existing app surfaces onto these tokens is separate, later work** with its own prompts and risk; nothing in the app uses the DS today. Layout: `styles.css` (imports only, fixed order fonts → colors → typography → effects → surfaces → motion → deck-motion → image-slot → data → surface-components → forms → sheets) + `tokens/*.css`, `tokens.js` (literal mirror for proofs), `assets.js`, `components/core/` (Button, Eyebrow, Divider, ChevronRail, TeamWordmark) and `components/brand/` (SealMark, MarkGlyph, Logotype, DeckFooter, ProgramLockup, SeasonLockup, FirstName, HudFrame, PlatePanel, StencilTitle, **DeckStage**), `components/data/` (Badge, Chip, SubteamBadge, Field, StatBlock, Readout, SpecTable/SpecRow, FocusTable/FocusRow, BarChart/Bar, GanttChart/GanttBar, DecisionMatrix, Timeline/TimelineItem, MatchClock, BuildCountdown, ScoutTable/ScoutRow, AllianceSplit), `components/surfaces/` (Card, Callout, SafetyNote, ImageFrame, Cutout, StepCard/Step, ProcessPipeline/PipelineStep, CompareSplit/CompareRow, SampleGrid/Sample, JumpGrid/JumpCard, CalloutDrawing/CalloutPin, QuoteBlock, RoleCard, PartCallout, FieldDiagram, SponsorWall/SponsorTier, AwardPlate, ResultBanner) `components/forms/` (Input, Select) and **`components/sheets/`** — the twenty-six full-sheet patterns that are the architectural core: CoverSheet, AgendaSheet, SectionSheet, StatementSheet, QuoteSheet, HubSheet, ClosingSheet, SplitSheet, GallerySheet, ProcedureSheet, ComparisonSheet, DataSheet, TimelineSheet, ScheduleSheet, SubteamStatusSheet, BlockerSheet, TargetsSheet, SafetySheet, RosterSheet, MatchBreakdownSheet, ScoutingSheet, FieldSheet, BOMSheet, AwardSheet, SponsorSheet, SeasonSheet (plus the internal `Sheet` frame) — each `.jsx` + `.d.ts` + `.prompt.md`, one demo card per group, plus `components/slots.jsx` (child-slot helpers), `SKILL.md` (Agent-Skill front matter) — `templates/Deck.dc.html` (the ONLY file anyone copies) + `templates/Specimen.dc.html` (all 26 patterns across 3 grounds and both audiences, REFERENCE ONLY — the audit fails if it is ever listed as a startingPoint), `_ds_manifest.json` (the registry and staleness authority), `specimen/` (the `/_ds` route), `README.md` (the full brand guide: content fundamentals, visual foundations, the FIRST usage rules, iconography, and the bundle manifest). Load-bearing rules, all machine-checked: the three ground scopes (`.frc-ground-squadron` default / `-field` / `-paper`) each declare the SAME 36 semantic aliases as LITERALS, and **`.frc-deck` SHARES the SQUADRON block** so SQUADRON is a real fail-safe default — a groundless deck root, or a sheet that omits its ground class, resolves all 36 rather than resolving partially (partial is the dangerous state: the raw palette still paints gold, so it looks almost right). One shared declaration, never a duplicated set; FIELD and PAPER override by inheritance; the shared block must stay ABOVE them in source order, because an element carrying `.frc-deck` and a ground class ties on specificity and source order decides. ds:audit check 14 enforces all of that plus "every `var()` in the token sheets resolves in every reachable state" (the 36th is `--surface-viewport`, the ImageFrame backplate, so a ground retints it and no component ever hardcodes it) (never `var()`, never only at the root — a root-declared alias freezes into every scope and a light sheet silently keeps dark values); Techmen Gold is illegal on paper (accent = bronze `#7A6300`, glow flattened to `none`); never invent a color (hex/rgb literals live only in `tokens/colors.css`, only from the published set); exactly four slide transitions (shutter/boot/banner/cut); every animation gated behind `prefers-reduced-motion: no-preference` and running only inside `[data-deck-active]` / `.frc-run` (base styles are the visible end state); `FirstName` enforces the FIRST name rules (caps+italic, first-use ® per heading/body channel via a layout-effect claim on an external-store registry — NOT a render-time claim, because `useId` differs across StrictMode's double render on a client root; refuses plural/possessive; throws on an external audience); deck chrome never shows a neutral white; no emoji, Lucide inline SVG only; Google Fonts is the ONE external host (a spec-mandated exception to the app's self-hosted-fonts rule). **Pass-2 rules, also load-bearing**: copy lives in children (every list-shaped component takes rows as child components and names further strings with a plain `slot` attribute; only structure stays an array prop — DecisionMatrix weights/scores, FieldDiagram zone geometry, chart scales); three image treatments chosen by what the image IS (ImageFrame opaque / ImageFrame `bleed` feathered / Cutout for alpha), where `bleed` drops the rim and brackets and **throws on a screenshot**, and Cutout draws no rectangle at all, grades with a filter chain on the subject, is always `fit="contain"` (throws on `cover`), and is `ground="none"` for every sponsor mark (SponsorTier throws otherwise); `tokens/image-slot.css` suppresses the platform's own `image-slot::part(frame)` grey wash **in the stylesheet**, never by patching a copied `image-slot.js`; **`variant="auto"` on MarkGlyph/Logotype switches on an INHERITED CUSTOM PROPERTY (`--mark-auto-gold` / `--mark-auto-black`), never a descendant selector** — a deck NESTS grounds (a paper sheet inside a squadron deck root), so `.frc-ground-squadron .frc-mark-auto > [data-variant="gold"]` matched from the OUTER scope however near the paper scope was, and paper's own rule only switched black ON, so both variants showed and Techmen Gold painted on paper; a custom property resolves against the NEAREST declaring ancestor, which is the rule that was always meant. It was invisible while every Type-*.svg slot was empty and became real the moment the logotype was wired; the red partition is contained — `--alliance-red`/`--alliance-blue` resolve ONLY inside `.frc-ground-field` and only in AllianceSplit, ScoutTable and FieldDiagram, off FIELD they fall back to structure tones and the RED/BLUE word carries the meaning, and MatchClock uses rust at zero rather than alliance red for exactly that reason; SafetyNote is its own component rather than a Callout tone so a safety scan is a glance; SubteamBadge imports `src/subteams.js` (one vocabulary, no per-value color map) and RoleCard mirrors the member_skills/skills model but takes props and children only, never a query. **RoleCard also owns its own LAYOUT**: `density="default"` puts the media beside the text at full scale, `density="compact"` (six or more cards on one sheet) puts it above and drops the type scale, padding and gaps by itself, and the wrapper class `frc-role-grid` carries the columns and the gutter. That trio exists because a generated deck placing nine RoleCards had to hand-write `gridTemplateColumns`, `padding`, `gap` and `font-size` from the deck — a deck reimplementing a component, which the inheritance rules forbid, and it happened because the component could not express what was needed. `slot="media"` (`slot="portrait"` still works) is optional and renders the system's marked `frc-frame-empty` affordance when unfilled, plus `slot="note"` for the paragraph that deck was inlining. **Pass-4 cleanup (spec v1.2)**: `DeckFooter` defaults to **`mark="logotype"`**, not the seal — v1.0 said seal in one section and logotype in another, and the logotype wins because it survives rail scale and does not duplicate the 5669 the rail already sets in type (`mark="seal"` stays available; both templates were updated). **`mark` now has a THIRD value, `"mark"`** — `MarkGlyph variant="auto"`, the winged helmet alone with no lettering, for a sheet whose body already sets TECHMEN in type. **The rail mark height went 32 → 56px**, logotype and mark alike so switching between them never shifts the rail: 32 is 2.2% of the 1440 frame, right at the usual 1/50 projection floor, and that floor is for plain type rather than artwork with hairline strokes and tight counters, so on a classroom projector the logotype read as a smudge. 56 is 3.9% of the frame, leaves 16px of clearance in the 96px rail (8px bottom padding), lands the logotype at 205px wide against the 200px FIRST zone beside it, and stays under that zone's 60px so a team mark never out-sizes the FIRST lockup on an external deck. `mark="seal"` keeps its own 64px. Measured in `/_ds`: computed 56 x 205px on all 32 footer rails, 0 console errors. **`DeckStage` is the one component that is behaviour rather than appearance, and every deck mounts it EXACTLY ONCE.** It replaces the stage script that used to live in `templates/Deck.dc.html`: it renders nothing visible and paints the canvas, the letterbox and the thumbnail frames from the **ACTIVE sheet's** `--bg0` and `--edge`, repainting on every sheet change. **`--edge` is the whole job** — a deck that paints only `--bg0` leaves everything outside the stage at the browser default, so a transition flashes white, which is exactly what the generated deck did. It does not own the deck root: it walks up to `.frc-deck` and READS, and six states trip the shared guard (no `.frc-deck` ancestor, no `.frc-stage`, no `data-aspect`, no ground class, no audience class, a second instance) — the middle four being what a Blank-generated deck lands in by default. It paints the DOCUMENT canvas only when the root carries `.frc-letterbox`, so an embedded deck (a demo card, a proof) leaves the host page alone. ds:audit check 15 requires it to be registered, to read both tokens, to import the shared guard, and requires its prompt to state the mount-once rule. **`DeckSteps` is its sibling, the deck's other behaviour component, and is also mounted EXACTLY ONCE.** It intercepts next/prev in the CAPTURE phase before DeckStage's document listener sees them, counts the active sheet's step group through `host.jsx`'s `structuralChildren`, and writes `data-step="n"` onto the SHEET — the one thing the reveal CSS is gated on. STEPPING IS OPT-IN PER SHEET, deliberately: automatic would silently re-pace every deck already written (nine cards that landed together would cost nine key presses) and the only escape would be a per-pattern opt-out prop, which is the thing this avoids. Two zero-API doors: `data-steps` on the sheet (every pattern already spreads `...rest` onto its root), which resolves the first of the six containers that already exist (`.frc-samples`, `.frc-steps`, `.frc-pipeline`, `.frc-role-grid`, `.frc-safety-list`, `.frc-subteam-grid`), or `data-step-group` on any container the author wrote. RE-ENTRY IS BY DIRECTION, NOT MEMORY: forward entry → step 1 (the only case that starts partial, because it is the only case where the presenter is pacing), backward entry → the LAST step, and every other arrival (thumbnail, Home, End) → the LAST step, because a jump is navigation and answering a question from the floor must never show eight of nine cards missing. Remembering per-sheet position was rejected — the same key would do different things on the same sheet depending on history. THE COUNT IS DONE IN JS BECAUSE CSS CANNOT DO IT: `:nth-child()` reads parentage and the runtime can wrap each item, so a `> :nth-child(n+4)` ladder would count host wrappers; items are marked `data-step-item`/`data-step-shown` and the stylesheet matches the mark by containment (same argument as check 29, one level in). THE BASE STATE IS STILL THE COMPLETE SHEET — `tokens/motion.css` says hide-until-clicked is prohibited and DeckSteps scopes that rule rather than repealing it: `data-step` exists only while a live DeckSteps is driving, `@media print` releases the gate outright, and unmount strips every attribute it wrote. That print release is what buys the ONE narrowing of check 22 (which otherwise fails on any rule-level `opacity: 0`/`visibility: hidden`), and control 30f holds it honest by moving the same declaration out of the gate and requiring it to fail. REDUCED MOTION STILL STEPS: the pending state is ungated, only the 0.28s fade-and-rise sits inside the motion gate — measured `transition-duration: 0s` with 4 of 9 revealed. `ds:audit` check 30 is shaped like 15 and is mostly about the ESCAPES (print release present and restoring visibility, nothing hiding outside the gate, the pending state not inside the motion gate, the six resolved containers all declared in the token layer, the shared guard and `structuralChildren` both imported, no bare `.children`), with 9 negative controls. **`.frc-letterbox`'s own CSS (`position: fixed; inset: 0; overflow: hidden`, plus `.frc-letterbox > .frc-stage { position: absolute }`) lived ONLY inside the two templates' inline `<style>` blocks until it was found and moved into `tokens/deck-motion.css`** — since nothing is copied any more, a deck that set the class per the routing header got a class `DeckStage` read correctly but that did nothing visually. Verified in headless Chrome against the shipped `styles.css` closure. **Guards no longer throw at run time**: `ImageFrame`, `Cutout`, `SponsorTier`, `SafetySheet` and `FirstName` render a visible rust fault marker (`components/guard.jsx`, `.frc-fault`) and throw ONLY inside the dev harness (`setHarnessMode`, on in `/_ds` and off during a capture run), because a guard that throws mid-presentation takes the deck down in front of the room on the external decks that matter most; FirstName's internal/external split is GONE — one guard behaviour in the system — and ds:audit now fails on a fault marker in a template. **The alliance containment scan is program-aware**, not hex-only: it reads the `--program` in force on the element, so the ProgramLockup rail is classified as a documented FLL/alliance-red value collision rather than a leak, and `/_ds` still prints it. **The three team marks AND the three logotypes are wired and provenance-pinned** (`assets/team/Mark-{Gold,White,Black}.svg` + `assets/team/Type-{Gold,White,Black}.svg` + `assets/PROVENANCE.json`, sha256 re-checked by ds:audit); every other slot is still empty and marked. **`MIN_SIZES.logotype` and `LOGOTYPE_RATIO` now carry the REAL aspect** (1049.669 x 287.139 = 3.6556) instead of the 5:1 placeholder that stood in while the slot was empty, so the rail box is the artwork rather than the artwork plus dead space. **THE SEAL WAS FETCHED, CHECKED AND REFUSED**: the canonical `5669-Seal.svg` carries `#ffe623` in all 71 of its gold declarations, not the published Techmen Gold `#FFE629` that Mark-Gold and Type-Gold both carry from the same page. A near-gold is exactly what the provenance check exists to catch, so `ASSETS.seal` stays null, `SealMark` keeps its marked empty slot, and the refusal is recorded under a new `rejected` block in PROVENANCE.json with the hash of what was refused. Because `DeckFooter` defaults to `mark="logotype"`, wiring Type-Gold cleared the empty box on every footer rail anyway; only CoverSheet, ClosingSheet and an explicit `mark="seal"` still show a slot. **The recolored app copy has since been swapped**: `public/assets/logos/Mark-{Gold,White,Black}.svg` are now byte-identical to the canonical files and registered in PROVENANCE.json as `mirrors`, which ds:audit hashes too — a mark that is correct in the bundle and wrong in `public/` now fails the audit. TWO KNOWN HAZARDS ARE RECORDED AND DELIBERATELY NOT FIXED: `src/theme.css` sets the APP's `--gold` to `#D4AF37` (not the published `#FFE629`), so the mark now renders canonical gold beside app chrome in the older gold — fixing it restyles every surface; and `npm run gen-icons` would overwrite three correct mark-derived PWA icons with solid `#005bff` squares, since the icons on disk were not produced by that script. **The harness boots from a clean checkout**: `.env.dsspec` and `.claude/launch.json` are tracked, `npm run ds:dev` boots it, and `App.jsx` lets `/_ds` through both gates in dev only, scoped to that one path. **`npm run ds:capture`** renders 26 patterns x 3 grounds x 2 audiences to PNG headless via playwright-core against the machine's Chrome (`/_ds?capture=…`), into `artifacts/ds-capture/` (gitignored); `--reduced-motion` emulates the preference. **Pass-3 rules, the sheet group**: every pattern INHERITS its ground and audience from the deck — no pattern names a ground, none takes an audience prop, and the audit fails on either (audience chrome switches in CSS via `.frc-audience-only-external` / `-internal` and the rail's FIRST zone); each composes the earlier groups rather than hand-rolling markup; each takes ALL copy as children and slots; each carries a default transition matching its role (10 shutter / 9 boot / 6 banner / 1 cut) that a deck overrides per instance, and the audit fails on a fifth; `HubSheet` is the one sheet with `footer={false}`, which is why it is also the one sheet with no FIRST mark — the rail is what carries 5669; `SafetySheet` THROWS without a `SafetyNote`; `MatchBreakdownSheet` is the FOURTH and last legal place for the alliance partition, and its scoping lives in `tokens/data.css` with the other three so `tokens/sheets.css` stays free of every ground selector. **Sheet patterns DISTRIBUTE their content row on a three-value axis assigned per kind in `tokens/sheets.css`** — `.frc-sheet-body` always handed `.frc-sheet-content` the leftover height as a 1fr row, and every kind then discarded it with `align-content: start`, which is why four gallery tiles sat in the top half of a 1440 frame with ~600px of dead ground under them. **start** (9 kinds: agenda, procedure, blocker, safety, comparison, bom, scouting, schedule, match) where the length of the list is the message; **center** (4: award, season, sponsor, split) where the content is a fixed composition with nothing to grow; **stretch** (8: gallery, roster, subteam, hub, field, timeline, targets, data) where height is readability. The five kinds with no content row at all (cover, section, statement, quote, closing) distribute in their own `.frc-sheet-<kind> .frc-sheet-body` rule — **StatementSheet's paragraph is centered and never stretched**. Stretch declares ROW ONE as the flexible track rather than stretching every row, so a `.frc-sheet-foot` or a stats band sharing the content row keeps its own height; DataSheet, the one stretch kind whose main block is not the first child, places both by class. `--fill-row` / `--fill-media-ratio` are INHERITED custom properties declared with `:where(.frc-sheet)` (so a kind class wins outright rather than by source order, the same argument check 14 makes about the ground scopes) and read by `.frc-sample` / `.frc-sample-media` in `surface-components.css` — that is the height-driven path that lets a tile drop its fixed 4/3 cap under stretch and keep it everywhere else. **The `align-items` relaxation sits at the FOOT of sheets.css on purpose**: it must out-order the per-pattern `align-items: start` pins it exists to relax, and it was measured LOSING that tie from the top of the file, leaving DataSheet assigned stretch and not stretching. Nothing in `components/sheets/` changed and no component was restyled. **`ds:audit` check 28** makes the assignment mandatory rather than defaulted (kind vocabulary DERIVED from the pattern sources, so a 27th pattern fails until its axis is chosen), and the `/_ds` **`sheet-fill`** section measures the lowest PAINTED bottom inside each body box in true 1920x1440 px. **HOST TRANSPARENCY (`components/host.jsx`) is the one mechanism that retires the direct-child assumption.** The Claude Design runtime wraps the template children of an `x-import` in layout-transparent host nodes, so a child the author wrote is NOT a direct child of the component it was written inside — and every place the system walked direct children or type-checked a slotted child assumed it was. That had already cost five separate workarounds, each patched where it hurt: DeckStage's sheet lookup (forked), the deck-motion direct-child selectors (patched in CSS), SafetySheet's note guard (a runtime shim), SafetySheet AGAIN (an entire sheet hand-built in JavaScript inside the deck rather than expressed as markup), and a permanent rule in every shipped deck hiding hosts that nothing bridged. ONE mechanism, TWO faces: a React-element traversal (`hostChildren` splices a wrapper away, `throughHost`/`hostProps` read the element the author wrote, `containsType` type-checks through one, `cloneThroughHost` clones the author's element and keeps the wrapper) and a DOM traversal (`isHostNode`/`structuralChildren`, where `display: contents` is a computed value and is read outright rather than inferred). `slots.jsx` routes every walk through it, which is why most components needed no edit; the sites that read a child directly do — SafetySheet, SponsorTier, CalloutDrawing/JumpGrid/ProcessPipeline numbering, FieldDiagram's zone labels, SubteamStatus's badge, and DeckStage. **LOOKING THROUGH A HOST IS NOT ACCEPTING ANYTHING**: the traversal descends through wrappers the RUNTIME inserted and through nothing else, so a hazard softened into a Callout and a SafetyNote buried inside a Card are both still refused — measured both ways. A host is recognised POSITIVELY by four signals in descending trust (explicit `data-frc-host`, inline `display: contents`, a custom-element tag or `<slot>` carrying no `frc-` class, `Type.frcHost`), never as "not one of ours", which would make every author element a host and every guard a rubber stamp. **THE WRITTEN-DOWN LIMIT**: a runtime whose host is an ordinary element made transparent by an EXTERNAL stylesheet is undetectable from React props; `data-frc-host` is the contract that closes that case, which is why it is listed first. **The CSS face is containment instead of parentage** — no combinator expresses "child, looking through display: contents", so the eight host-crossing selectors (`.frc-letterbox > .frc-stage`, `.frc-stage > .frc-sheet` x3 incl. print, `.frc-frame-plate > img|image-slot|.frc-frame-media`, `.frc-cutout-subject > img|image-slot`, `.frc-sample-media > img`, `.frc-role-grid:has(> .frc-role…)`) became descendant matches. That is EXACT, not approximate: on every one the child cannot legally nest inside itself within that parent, so the two forms agree on every legal tree. The `>` selectors that do NOT cross an author boundary were audited one by one and deliberately LEFT ALONE (`.frc-slot > img`, `.frc-mark-auto > [data-variant]`, `.frc-cutout > .frc-frame-empty`, `.frc-role-media|.frc-role-portrait > .frc-frame-empty`, `.frc-sheet > *:not(.frc-ambient)`, and `.frc-sheet-content > :not(.frc-sheet-foot, .frc-data-stats)` — all 26 patterns were read to confirm that content-row children are system-rendered wrappers, never author children). **ds:audit check 29 holds BOTH ends** (the mechanism ships its whole surface and identifies hosts positively; every component that walks or type-checks a child imports it; DeckStage never reads `stage.children`; no token-sheet `>` crosses the boundary list) — because a JS traversal that looks through hosts while the stylesheet still demands parentage is half a fix, and the missing half is invisible until a deck ships. Check 16's required selector moved to `.frc-letterbox .frc-stage` to match. Verification: `npm run ds:audit` (static: alias sets, literal-only values, published colors only, import order, motion gate, manifest accuracy, no emoji; the manifest registry is 79 components / 6 demo cards, plus the sheet-pattern ground, audience and transition rules and the specimen's never-a-starting-point status). **`ds:audit` now runs 30 checks: 1-15 are the system's own invariants, and 16-27 are the SOURCE-SIDE COUNTERPARTS of the numbered pre-delivery checks in `docs/FRC_CLAUDE_DESIGN_STANDARDS.md`.** The two audits number INDEPENDENTLY and are always named when referenced ("pre-delivery check 43", "`ds:audit` check 16"); both run, because source cannot see what a deck added and a deck cannot see whether the rule it relied on ships. 16 is the one with a shipped regression behind it: the deck chrome contract (`.frc-letterbox`, `.frc-letterbox > .frc-stage`, `.frc-thumbs-dock`, the stage, the thumb rail) must be declared in `tokens/`, and no template's inline `<style>` may declare an `frc-` rule at all — a template is reference nobody copies, so a rule there ships to no deck. The rest: 17 every motion class the standard's inheritance block names has a rule (the motion vocabulary slice is pinned by sha256, so editing it fails the audit rather than drifting; the slice is located from the `INHERITANCE, NON-NEGOTIABLE` anchor rather than by a bare first-occurrence search, because the standard QUOTES both slice markers where it describes the check, and under a bare `indexOf` a doc edit above the block resliced check 17 and failed blaming MOTION_VOCAB — reproduced, then fixed, and it now carries a POSITIVE control), 18 no parallel motion vocabulary, 19 gold is never the RESTING color of body copy, 20 alliance containment, 21 the ambient footer-zone clip, 22 nothing hidden in the base state, 23 no running copy in an array prop, 24 the guard registry, 25 LIVE/REC is the ground accent, 26 small radii, 27 no neutral white surface. **Each is narrow on purpose and every narrowing is written down at the check**, because a check that fires on legitimate code gets commented out and is then worse than not existing: 19 skips state-qualified selectors (the doc grants gold to active state, and ComparisonSheet's `[data-lead]` cell is exactly that), 22 judges `display: none` only on body copy (a paired-restore rule was tried and fires on audience chrome, divider variants and the paper bloom suppression), and 23 tests the ELEMENT TYPE so `DecisionMatrix` weights and `FieldDiagram` zones are out of scope by construction rather than by exemption. ONE ITEM WAS NOT MECHANIZED AND IS RECORDED AS SUCH: the source-side counterpart of pre-delivery check 21, FIRST never typed by hand, needs a JSX parser to know the enclosing element — a regex version trips on the legal `<FirstName>FIRST Robotics Competition</FirstName>` and on both templates' deliberately hand-written static markup, so it stays a pre-delivery check. **`npm run ds:audit:controls`** is the negative-control harness: it breaks each of 16-27 in turn (31 controls, one of them POSITIVE — a legal doc edit that must NOT trip check 17), asserts the right outcome, and restores the file; it refuses to run on a dirty tree and the dev-only `/_ds` route — `App.jsx` creates its lazy import only when `import.meta.env.DEV`, so the chunk is absent from `dist` (verified by grep) and production renders a tiny 404 element; it touches no auth and no Supabase and MOUNTS THE REAL COMPONENTS, proving in-browser: alias resolution in all three grounds, no rendered gold on paper, all four transitions, gate coverage, static end state, no neutral white in chrome, every sheet pattern stepped through all three grounds and both audience modes with ground, audience and ambient switchers. Running `/_ds` locally still needs the app shell to boot (`src/supabase.js` throws without `VITE_SUPABASE_URL`); a placeholder `.env.dsspec.local` + `vite --mode dsspec` is enough (gitignored). **Most assets are still NOT in the repo**: the FIRST program PNGs and the seal are expected under `src/lib/design-system/assets/` (exact list in `assets/README.md`); components render marked empty slots at the correct minimum size until they land. **The SEASON artwork IS wired**: `assets/season/season-lockup.png` is the FRC **2027** season logo, **BIOCORE presented by Haas**, taken byte-for-byte out of `first-biocore-logos.zip` (the `Digital RGB` vertical full-color PNG, 1730x1230) from the FIRST season brand downloads page and renamed only — the path stays generic because replacing that one file is the entire annual reskin. Both the archive and the extracted file are hashed in PROVENANCE.json, so ds:audit check 13 pins it like a team mark. FIRST usage rules apply in full: used as supplied, never recolored, rotated, skewed, cropped, bordered or captioned — measured in `/_ds` at `object-fit: contain`, `filter: none`, `transform: none`, so SeasonLockup's 240x240 default letterboxes it rather than distorting it. The artwork carries its own filled plates, so one file serves all three grounds and no reverse variant is needed; its yellow element is FIRST's yellow, not Techmen Gold, and the no-gold-on-paper rule does not reach supplied artwork. `public/assets/logos/Mark-{Gold,White,Black}.svg` exist for the app splash but were deliberately not wired (provenance unverified; wiring is three lines in `assets.js`). Nothing in the spec's component manifest is pending; only the asset files are outstanding. **The DS is SYNCED to Claude Design** (project `FRC 5669 Techmen Design System`, `0ae827ec-d481-4eb8-a051-772544deb9c4`, pinned in `.design-sync/config.json`) — **52 of 78 as of 2026-08-24**: Core 5 + Brand 11 + Data 16 + Surfaces 18 + Forms 2, each with a graded preview card, `.d.ts` and `.prompt.md`. **The 26 sheet patterns are still unsynced** — they ship in the JS bundle and are usable off the global, they just have no card or types, so the design agent composes decks without seeing the sheet layer. **`componentSrcMap`/`docsMap` are now GENERATED from `_ds_manifest.json`** rather than hand-enumerated, which retires the old silent-rot risk; widening scope is one `GROUPS` list. `.design-sync/conventions.md` is the `readmeHeader` — the conventions block prepended to the uploaded README and inlined into the design agent's system prompt; it is human-editable and a later sync VALIDATES its names against the fresh build rather than rewriting it. Sync state, the hand-enumerated `componentSrcMap`/`docsMap`, and the authored preview cards live in `.design-sync/` (committed); `ds-bundle/` (the built upload) and `.ds-sync/` (vendored converter + playwright) are gitignored and must be rebuilt on a fresh clone after `npm i --no-save @types/react`. **CONFIRMED: `templates/` cannot be shipped to Claude Design, and the docs were corrected rather than the pipeline forced.** The converter has no template concept at all — not a missing setting; the whole `cfg.*` surface was enumerated and the words "template"/"startingPoint" appear nowhere in it. The platform DOES support templates (the IDEA design system in the same account has four working ones): a `templates/<slug>/` directory holding the entry file, its scripts, its images and a captured `.thumbnail`, registered in the platform-compiled `_ds_manifest.json` under **`templates[]`** — **NOT `startingPoints`**, which is empty even in IDEA, so our own manifest's `startingPoints` key is local vocabulary matching nothing upstream. Hand-uploading `templates/deck/Deck.dc.html` was TESTED: the file landed, the `_ds_needs_recompile` fence was consumed (the self-check ran), and the recompiled manifest still reported `templates: []` — no template in the picker, no Templates nav section. Probe deleted. A working folder would also need a `.thumbnail`, a root `thumbnail.html`, and a vendored copy of the platform's generated `support.js` dc-runtime ("do not edit"), so it was not forced. **Both templates are now READABLE REFERENCE, neither is a starting point**, and `startingPoints` has been DROPPED from `_ds_manifest.json` entirely (the platform reads `templates[]`; that key described a route that does not exist). A deck starts from Blank and assembles out of the library. **The shell's stage script is now the `DeckStage` component** — see the DeckStage bullet. `templates/Deck.dc.html`, `templates/Specimen.dc.html`, `README.md`, `SKILL.md` and `_ds_manifest.json` all say this now, and ds:audit check 11 holds it. Measured consequence on a real generated deck: the root carries `.frc-deck` but **no ground class**, which does not break rendering (the raw palette is on `:where(.frc-deck, .frc-ground-*)` so `--gold` still resolves `#FFE629`, every sheet carried its own ground, and the deck-stage script paints from the ACTIVE SHEET) but leaves no alias fallback for content placed directly in the deck root — **now closed by the `.frc-deck` fail-safe above** (the `:root` alternative was correctly rejected: the scoping is load-bearing against `theme.css` and it measured correct). **What the Blank path actually loses**, read from the generated deck's own source: the 4:3 1920x1440 stage is ABSENT (`data-aspect="16:9"`, no `1440` anywhere); the letterbox is ABSENT (the string `letterbox` does not occur); the deck-stage script is ABSENT as written (`frcDeckStage` and `getComputedStyle(sheets[current])` both absent) and only partially re-created — the deck rolls its own keydown/resize/scale and reads `--bg0` but never `--edge`, so the one behaviour the script exists for, painting canvas and letterbox from the active ground so a transition never flashes white, is gone; the audience class is ABSENT; the thumbnail rail is ABSENT. The **footer rail is PRESENT** — not from the template, but because `DeckFooter` is a component, which is exactly why the rail, the 5669, the deck name and the sheet counter all rendered.
- **Weekly survey** (`supabase/weekly_surveys.sql` + `src/surveys.js` + `src/SurveyPage.jsx`/`.css` + `src/SurveysAdmin.jsx`/`.css`). A short recurring pulse check students fill out on a phone in the shop, SEPARATE from the per-season `member_applications` onboarding form. Four tables: `surveys(title, opens_at, closes_at, is_open, created_by, created_at)`, `survey_questions(survey_id, position, kind[single/multi/scale/text], prompt, options jsonb, required)`, `survey_responses(survey_id, member_id, submitted_at)` unique `(survey_id, member_id)`, `survey_answers(response_id, question_id, value jsonb)` unique `(response_id, question_id)`. **QUESTIONS ARE ROWS, NOT A HARDCODED FORM** — the fifth seeded question is about one October competition and rotates weekly, so nothing about any prompt, option or kind exists in the client. **`surveys_one_open_idx` (`unique on ((true)) where is_open`) makes "the single currently-open survey" a DATABASE guarantee** rather than a client tiebreak; the consequence is that opening B while A is open RAISES, so `SurveysAdmin` closes the open one first as two writes — a trigger would have hidden the rule, and a race between two mentors now ends in an error rather than a second open survey. **`survey_questions_required_chk` enforces `required = false or kind = 'single'`** — only single-choice may be required, written where it cannot drift; the member surface re-states it rather than trusting the row. **RLS**: surveys and questions `select using (true)` to authenticated, write `is_staff()` (the repo's existing mentor check — no new role helper); `survey_responses insert own` is `member_id = auth.uid() AND the survey is_open` (without the `is_open` clause a member could POST into last month's closed dataset); select is own-or-staff on both responses and answers; `survey_answers insert own response` matches through the caller's own response row. **NO UPDATE AND NO DELETE POLICY on responses or answers, for anyone including staff** — a submitted answer is a record of what somebody said at the time, so **editing a submission is NOT OFFERED** (the brief's other option, a server-side resubmit-that-replaces, would need a SECURITY DEFINER RPC and is deliberately not built). `revoke update, delete, truncate ... from authenticated` is required for the same reason feedback.sql needs it — the bootstrap `ALTER DEFAULT PRIVILEGES` means policy absence alone fails SILENTLY at 0 rows. **`survey_answers_nonempty_chk` rejects `null` / `""` / `[]`**, which is "a skipped question stores NO ROW" expressed in the schema, so the mentor aggregate never has to filter blanks. **`src/surveys.js` is the shared pure module** (no React, no Supabase): `resolveOpenSurvey` / `sortQuestions` / `isAnswered` / `buildAnswerRows` / `missingRequired` / `isUnconfigured` / `aggregate`. `aggregate`'s percentages are **of ANSWERED, not of everyone who submitted** — every kind but a required single is skippable, so the submission count is the wrong denominator; options render in the mentor's AUTHORED order (never sorted by count) so an option keeps its place week to week, a zero-count option still gets a row, and a value no longer in the option list is appended and marked `unlisted` rather than silently dropped. **`/survey`** is the member surface: one question per card, full-width 48px tap targets, **no `<select>` anywhere**, one scroll and one button; tapping a chosen single option again CLEARS it (an optional single otherwise has no way back to no-answer); a 23505 from the unique constraint resolves to the already-submitted state rather than an error; already-submitted is an explicit card, never a blank form. **A required question whose options are still a TODO is rendered as a visible "no options yet" marker and is NOT enforced as required** — the seeded survey ships in exactly that state, and a form that refuses to submit with no way to fix it from the member's side would be worse. **`/surveys`** is the staff surface: create, author questions with kind chips and reorder, open/close, and results — aggregate by default with **individual attribution behind a deliberate toggle**, because putting names on by default changes what people are willing to type into "what is blocking you". Seeded survey "Week of Aug 31" is open with five questions; **questions 1 and 2 ship with their options as a TODO comment** and must be filled in before it goes out. **`opens_at` on the seed is deliberately NULL** — it was written as 2026-08-31 first and the member surface then correctly reported "no survey open" when pasted on the 30th. Nav: "Weekly Survey" beside Study in the avatar menu (non-parent members), "Surveys" in the staff block. **The MANAGEMENT surface landed 2026-08-30** on the `/surveys` Settings tab: a list that carries each survey's state, window, question count and RESPONSE COUNT (eagerly, in the same `loadSurveys` round trip as the list itself -- the response count is what makes a delete safe to press, and a count behind a click is a count nobody looks at), plus rename / re-window, **duplicate**, **CSV export** and **delete**. **Duplicate is the load-bearing one**: the normal week is duplicate last week, swap the rotating question, open -- without it every week is a re-author from an empty form, which is how a weekly survey becomes a monthly one. It copies title (suffixed, counting up), questions, options and required flags, RENUMBERS positions 1..n, copies no responses, and **lands CLOSED, which is exactly what makes it safe while another survey is open**: `surveys_one_open_idx` is partial `where is_open`, so a closed row is not in the index at all. **Delete is two clicks and the second one names what dies by number** ("Delete 'Week of Aug 31'? 3 responses and 7 answers will be deleted."), never a bare are-you-sure; when the armed survey is the LIVE one it says so as well. **The delete relies on ON DELETE CASCADE reaching past the `revoke update, delete, truncate ... from authenticated`, and that was MEASURED against a real PostgreSQL 16 rather than reasoned about** -- a cascade runs as the referencing table's OWNER, so the caller's table grants and RLS are not consulted for the rows it removes; the direct deletes still raise 42501, which is the control that makes the cascade result mean something. The evidence and the "do not fix this by granting delete on the children" warning are recorded at the revokes in the migration. **`surveyState()` is why the pill is not just `is_open`**: is_open true with a passed `closes_at` reads EXPIRED and with a future `opens_at` reads SCHEDULED, because in both cases the member surface shows nothing -- calling those OPEN is the exact confusion the seeded survey caused. **The CSV's Member column is wired to the SAME attribution toggle as the on-screen names**: the column is always present so the sheet's shape does not move, but the values only fill in while the surface is already showing names, because a CSV is the easiest way for an anonymous aggregate to quietly become a named one. One row per response, one column per question, `submitted_at` as the stored ISO instant. **NOT BUILT, deliberately: no edit-after-submit, no reminder push (the `announcements` prefs key exists but has no feature), no per-season scoping, no scale question in the seed** (the kind is supported and authorable, nothing uses it yet)**, no CSV formula-injection guard** (a leading `=` in a student's free text is emitted verbatim, because the export is a record of what was said), **no undo on delete, and no archive-instead-of-delete**.
- **CI: `.github/workflows/integrate.yml`** (added 2026-08-30 -- this repo had NO workflows at all before it). On push to `claude/**`: checkout at full depth, `npm ci`, `npm run build`; on green it merges the branch into `main` with `--no-ff` and deletes the branch, on red it does nothing else. **A standing `claude/**` branch IS the failure signal** -- there is no notification to wire up and nothing to go and read, the branch list is the report. **It never force-pushes**: the merge is a plain push, so if `main` moved underneath the run the push is rejected and the job fails with the branch still standing, which is the same signal and the right one. **`main` gets NO CI run of its own from this workflow**, and that is a GitHub rule rather than an omission: pushes made with the default `GITHUB_TOKEN` do not trigger further Actions runs (the loop guard), so the merge commit starts nothing. The build that gated the merge ran on the BRANCH, before the merge, and is the only run that code ever gets. Giving `main` its own CI needs a separate `push: branches: [main]` workflow (which still will not fire for merges made here) or a PAT/App credential in place of `GITHUB_TOKEN`. Both facts are written into the workflow file itself. One `concurrency` group per branch, `permissions: contents: write`.
- **In-app feedback + admin inbox** (`supabase/feedback.sql` + `supabase/feedback_rls_test.sql` + `src/FeedbackWidget.jsx`/`.css` + `src/FeedbackPage.jsx`/`.css`). A global "send feedback" button on every authenticated page, and an admin-only inbox at `/feedback`. **`public.is_admin()` is NEW here** — same shape as `is_staff()` from `skills_catalog.sql`, narrowed to the admin role; it exists because this is the first table whose read policy is admin-only rather than staff-wide, and nothing else was repointed at it. `feedback(member_id, category[bug/idea/feedback], message, image_paths jsonb, route, viewport, user_agent, status[open/reviewed/dismissed], reviewed_by, reviewed_at, created_at)`. **RLS: insert own (`member_id = auth.uid()`), select/update `is_admin()`, and NO DELETE POLICY FOR ANYONE** — an inbox is triaged, never pruned. Two consequences written down at the policies: the member who filed a report **cannot read it back** (one narrow policy instead of two overlapping ones; acceptable because the widget has no history view and no edit-after-submit), and `revoke delete, truncate ... from authenticated` is required because the bootstrap `ALTER DEFAULT PRIVILEGES` grants authenticated everything on a new public table — without it a stray delete fails SILENTLY at 0 rows instead of raising. **Storage bucket `feedback` is PRIVATE, deliberately unlike `jobs`**: the jobs bucket is public because it holds reference photos of parts, whereas a feedback screenshot is a picture of the app mid-use and routinely frames other members' names, hours and contact rows. So read is `is_admin()`, insert is authenticated (bucket_id only, same shape as jobs), delete is uploader-or-admin — and the admin page must mint `createSignedUrl`s (600s TTL, re-signed on every list load, never cached indefinitely) because a private bucket has no public URL at all. **MEASURED CAVEAT recorded at the policy: the `owner = auth.uid()` half of the delete rule is DEAD for a non-admin**, because a DELETE's row lookup goes through the admin-only SELECT policy, so their own delete matches 0 rows; a member's images orphan in the bucket if the row insert fails after upload, and only an admin can clear them. **The widget is mounted ONCE in `App.jsx`'s `ProtectedLayout`**, lazy with its OWN `<Suspense fallback={null}>` — sharing the app-level boundary would put the whole shell back on the splash while it loads. Three image-input paths all funnel into one queue (`addFiles`): file picker, drag-and-drop, and **paste bound to the whole panel rather than just the textarea**, so a clipboard screenshot lands whether or not the caret is in the message box. Telemetry (route, viewport, user_agent) is gathered at submit time and `member_id` comes from the session — the member fills in nothing but category and message, and **no category is preselected** (the submit stays disabled until one is). Images upload first, then the row is inserted carrying their paths. **`/feedback`** is `isAdmin`-gated in App.jsx and lives in the NavBar avatar menu next to Readiness with an open-count badge, admin-only (`isStaff` would be wrong: the ask was "as an admin", and a report can quote anything the member had on screen). It filters by status/category/text, shows a thumbnail strip, and opens an ApplicationsPage-shaped `fbp-modal-*` detail with full telemetry, full-size images and mark-reviewed / dismiss / reopen (a plain client update — this table is admin-owned end to end, so the RLS policy is the whole authorization story and a SECURITY DEFINER RPC would have nothing to enforce). **"Copy for Claude"** writes a plain-text block (category, author, timestamp, route, viewport, user agent, status, the message verbatim) to the clipboard, per row and as a multi-select `Copy N as prompt` that concatenates blocks oldest-first with a rule between them; **it deliberately embeds NO signed URL** — one would be dead by the time anyone read the pasted prompt, and scattering a link to a screenshot of other members' data into a chat log is an unnecessary exposure, so it states the image count and where to view them. Selection is scoped to what is on screen, so a filter change drops a hidden row rather than silently carrying it into the bulk copy. **The page's classes are `fbp-` and the widget's are `fb-`** — both stylesheets load together on `/feedback`. **EXPLICITLY NOT BUILT: no automatic client-error capture, no email/Discord notification (this is a pull inbox), no edit-after-submit, no app-version/commit-sha telemetry.** **The check-in fast paths do NOT get the widget** — `/checkin` and `/checkin-volunteer` sit OUTSIDE `ProtectedLayout` in the route table, which keeps the NFC path light as the architecture rule requires.
- Push notifications (`supabase/push_notifications.sql` + `supabase/functions/`). Web push, opt-out per category, sensible defaults, quiet hours, batched + deduped. **SW moved to vite-plugin-pwa `injectManifest` mode** with a custom `src/sw.js` (precaches `self.__WB_MANIFEST` — offline preserved; `skipWaiting`+`clients.claim` keeps autoUpdate-on-reload; handles `push`/`notificationclick`). Schema: `push_subscriptions(endpoint pk, member_id, p256dh, auth, …)` own-row RLS; `profiles.notification_prefs` jsonb (master `enabled`, `task_signoff`/`event_reminder`/`parent_digest` on, `shop_status` OFF, `quiet_hours` 21:00–07:00 in America/LA); `events.mandatory` bool; `notifications_sent(member_id, kind, ref_id)` dedupe ledger (RLS locked, service-role only). Edge Functions (`send-push` + `cron-notify`, each self-contained/single-file so they deploy via the Dashboard editor, using `npm:web-push`, gated by an `x-push-secret` header, deploy with JWT verification off): prefs/quiet/dedupe checks, VAPID sign, prune 404/410. Triggers: a `task_claims` AFTER UPDATE trigger (`notify_task_signoff`) calls send-push via `pg_net` on the verify_task transition (completed=approved, →claimed=rejected); pg_cron calls cron-notify for batched event reminders (≤24h, RSVP'd going/maybe OR mandatory, one per member/day), the parent daily digest (8pm PT, one per parent), and optional shop_status. More triggers in `supabase/notification_triggers.sql`: **schedule_change** (statement-level events INSERT/UPDATE/DELETE triggers → broadcast to schedule_change-opted members; series-coalesced — bulk insert/series delete are one statement = one push, series edit's N updates collapse via a shared `sched:edit:<series_id>:<day>` ref_id in the dedupe ledger); **skill_signoff** (member_skills→certified trigger, mirrors notify_task_signoff; member_skills has no pending state so only certify fires); **checkin_reminder** (evening pg_cron → cron-notify reminds anyone with an open check-in, once/day, default off). **Catalog (the things that send): task sign-off→claimant, event reminder (batched), schedule changes (series-coalesced), shop open/closed (opt-in), skill sign-off→member, check-in reminder (evening, opt-in), parent daily digest. `job_assignment` + `announcements` prefs keys exist but their toggles are HIDDEN in the UI (no underlying feature yet — un-comment in NotificationsPanel CATEGORIES to restore). Explicitly never: job-claimed, generic activity, individual check-ins.** `/checkin` imports none of it. **Deploy is held until the rest is live** — see the DEPLOY block in the SQL: needs VAPID keys (`VITE_VAPID_PUBLIC_KEY` frontend + `VAPID_PUBLIC_KEY`/`VAPID_PRIVATE_KEY`/`VAPID_SUBJECT`/`PUSH_SECRET` function secrets), the two functions deployed `--no-verify-jwt`, and the `private.push_config` row filled (until then the trigger/cron no-op).
- Multi-claim jobs (`supabase/multi_claim_jobs.sql`, refactors jobs_board). Per-claimant lifecycle lives in `task_claims(task_id, member_id, status[claimed/submitted/completed], …)`, PK (task_id, member_id). `tasks.max_claimants` (int, default 1 = solo, null = unlimited, N = capped). `tasks.status` is now staff-controlled **availability** (open/closed/completed), no longer auto-driven by a single claim. RLS: task_claims readable by all authenticated, staff write/delete; members transition only via the SECURITY DEFINER RPCs. **claim_task locks the tasks row FOR UPDATE before counting claims (race-safe capacity).** **Cert-gating is collective (`supabase/jobs_collective_certs.sql`)**: required certs are covered by the *group* of claimants, not one person. A member may claim a cert-gated job if they hold **at least one** required cert (none → blocked); the job is "fully staffed" when the union of claimants' certs covers every required cert. The Jobs detail shows per-cert coverage (which claimant covers each, which are uncovered) and the list row shows a "⚠ N cert gap" badge until covered. JobsPage loads claimants' certified `member_skills` into `certsByMember` to compute coverage client-side. **verify_task signature changed to `verify_task(p_task, p_member, p_approve)`** — per-claimant sign-off; update every caller. Staff close/complete/reopen by updating `tasks.status` directly. **Job creation is open to all members (`supabase/jobs_open_create.sql`)** — the "+ New job" button + create form show for any signed-in member, and a permissive `tasks insert own` RLS policy (`with check created_by = auth.uid()`) sits alongside the staff for-all policy; a matching `task_skills insert own task` policy lets a creator attach required certs to their own task. **Edit/delete stay staff-only** (no member UPDATE/DELETE policy; the edit form is reachable only via the staff actions). **Deprecated (kept, not dropped — live data): `tasks.claimed_by`, `tasks.verified_by`** — unused by the new logic. `readiness_summary.sql` was updated to read task counts from `task_claims` (re-run it alongside the migration). **Jobs page browse UI**: compact collapsible list rows (status pill, title, due/claim/cert meta) grouped by subteam; clicking a row opens a detail modal with the full job + all claim/staff actions. Title search + sort (status/due/cert/title). Status is a derived color scale — open=green, in progress=amber (≥1 active claim), needs-review=red (≥1 submitted claim awaiting sign-off), completed/closed=gray (there is no literal "blocked" task state; needs-review is the red bucket). `tasks.due_date` (date, nullable, `supabase/tasks_due_date.sql`) added with a form field + overdue flag. **Job content + admin controls (`supabase/jobs_content.sql`)**: `tasks.links`/`tasks.images` (jsonb arrays) for reference links + uploaded images shown in the detail; `task_updates` (progress thread: body + optional image, posted by a claimant or staff, RLS-gated); **time tracking reuses attendance** — `attendance_events.job_id` stamps a check-in session with a job, set by the member via the `set_session_job(p_task)` RPC ("I'm on this job" button: requires an active claim + being checked in; links the current open session), total per member per job shown in the detail; `admin_revert_claim(p_task, p_member)` (admin only, confirm dialog) undoes an approved/completed claim back to 'submitted' and reopens the job if it was completed. **First use of Supabase Storage**: public bucket `jobs` (paths `task/<id>/…` and `update/<id>/…`); public read, authenticated upload, owner/staff delete — policies in the same SQL file (if `create policy on storage.objects` errors on ownership, create the bucket + policies via the Storage UI instead).
- Installable PWA, OTP + Google OAuth login.
- Check-in/out with NFC deep-link fast path. Geofence at 34.041550 / -118.086826, 150m radius (check-in gated to location, check-out always allowed). **iOS reliability**: check-in geolocation fires from a "Confirm check-in" tap (a gesture), not on load (`verifyAtShop` uses `enableHighAccuracy`/20s/`maximumAge:0` and rejects fixes coarser than 100m as `reason:'imprecise'`). **Hour category at check-in**: the `/checkin` confirm screen shows a category picker (chips) defaulting to **build**; the chosen category is written to `attendance_events.category` on the IN event only (the OUT just closes the open session — the hours math attributes a session by its IN category). Categories live in **`src/categories.js`** (tiny, no React/Supabase deps so the fast path stays light): `build`(default)/`outreach`/`volunteer`/`competition` (reduced from six — `fundraising`/`mentoring` were dropped, see `supabase/categories_reduce_event_kind.sql`), each with an `--hr-*` theme color; helpers `categoryLabel`/`categoryColor`/`normAttendanceCategory`(legacy `'normal'`/null→`build`)/`loggedTypeToCategory`/`emptyBreakdown`. **Per-student geofence exemption**: `profiles.geofence_exempt` (bool, default false) — staff toggle it on the roster (`Geo exempt`, direct `profiles.update`, admin RLS); an exempt member's check-in skips `verifyAtShop` and inserts directly (check-out unaffected). **Volunteer check-in (`/checkin-volunteer`, `VolunteerCheckinPage.jsx`)**: a second NFC fast path for summer FLL-room volunteering — same minimal layout + `CheckinRedirect` pending-URL bounce, but each session it opens is hardcoded to `attendance_events.category = 'volunteer'`. The category column is set by **`supabase/hour_categories.sql`** (supersedes the old `attendance_category.sql` normal/volunteer pair and the removed `volunteer_hours_backfill.sql`; default `'build'`), and the check constraint was later reduced to the four current categories by **`supabase/categories_reduce_event_kind.sql`** (`check in ('build','outreach','volunteer','competition')`; re-tagged existing `fundraising`/`mentoring` → `outreach`) — **must be applied or category-tagged inserts fault**. Same-tap toggle checks out an open volunteer session; tapping while a non-volunteer session is open auto-switches (closes it with an OUT, opens volunteer). Its geofence is the **FLL room (34.042134 / -118.086326)** via `verifyAtFLL`; `geo.js` now shares one `verifyAt(lat, lon)` core (same 150 m radius + 100 m accuracy floor + `enableHighAccuracy`/20s/`maximumAge:0`) behind `verifyAtShop`/`verifyAtFLL`. Both check-in paths insert client-side (there is **no** check-in RPC); RLS own-row is the server-side enforcement.
- Presence Board (`/display`, staff-opened wall display): read-only, who-is-present derived from open check-ins, subteam-grouped, live via 15s polling (realtime is not wired in the project). Renders **inside ProtectedLayout** so the NavBar + avatar stay visible.
- Kiosk check-in — **REMOVED** (was a shared-tablet Web NFC toggle via `staff_override_attendance`). The `/kiosk` route, `KioskPage.jsx`/`.css`, nav entry, and context tag were all deleted (it had been deactivated/commented for a while). `staff_override_attendance` RPC + `src/presence.js` remain (presence.js still used by PresenceBoard); restore from git history if ever needed.
- Auto-checkout at 10 PM via pg_cron; auto-closed sessions are flagged for mentor review before counting.
- Public landing page. NavBar: primary row is Dashboard / Schedule / Jobs / Hours ▾ / Skills, each with an inline-SVG icon (self-hosted icon set in NavBar.jsx, themed via `currentColor`; no icon dependency). **Hours ▾ co-locates the hours tools**: My Hours, Log Hours, then a staff-only divider + Team Hours / Verify Hours / Reports (Verify Hours + Reports render only for `isStaff`; non-staff see just My Hours / Log Hours / Team Hours, no divider). **Skills** is a plain link → `/skills` for non-staff, but a **dropdown for staff** (Catalog `/skills` / Certify `/certify` / Coverage `/coverage`). **Study lives in the avatar menu** (members/non-parent), not the top row. The **parent branch** (`hasRole('parent') && !isStaff`) shows only Dashboard / Schedule / Team Hours. **Avatar menu**: My Profile, Study, Replay tour, then (if `isStaff`) a divider + **Readiness** (no header) + a **People** group (Roster — *only if `isAdmin`* — , Access Requests w/ pending badge, Squad) + a **Live** group (Activity, Display), then Sign out. Group labels are non-clickable `.nav-dropdown-section` headers. (Verify Hours/Reports/Certify/Coverage moved OUT of the avatar menu into the Hours/Skills dropdowns.) Roster is admin-only so it only renders for admins, fixing non-admin staff hitting permission-denied. The avatar menu right-anchors via `.nav-dropdown-menu.nav-avatar-menu` (the doubled-class selector is load-bearing: it out-specifies the base `.nav-dropdown-menu { left:0 }` so the menu can't overflow the right edge) and scrolls on short viewports. A gold dot on the avatar flags pending access requests for staff (`isStaff && pendingAccess > 0`). **Kiosk is fully removed** (route, page, CSS, nav entry, context tag all deleted).
- My Hours and Team Hours board (aggregates attendance events live). **Hour categories** are the single axis: `build`/`outreach`/`volunteer`/`competition` (four; `fundraising`/`mentoring` were removed), defined once in `src/categories.js` (the `CATEGORIES` array + `--hr-*` color per key), color-coded consistently across both boards. **Aggregation** (`buildBreakdown` in hoursUtils): each attendance session is attributed to its **IN** event's category (normalized; legacy `'normal'`/null → `build`); attributing by the IN side keeps it robust to the auto-close 'out' (which need not carry one). Verified `logged_hours` rows fold into the same buckets via `loggedTypeToCategory` (`volunteering`→`volunteer`). `buildBreakdown`/`sumBreakdown` return `{ ...all four categories, total }`; selects feeding it pull `category` (HoursBoard, MyHoursPage); `sessionsFromEvents` carries the (normalized) per-session category. Generic worked-time totals (`computeHoursMs` → HomePage/ParentHomePage Today/Season, RosterPage sort) remain full all-category totals — they're unlabeled aggregates, not a per-category bucket. **Team Hours displays the full split**: a category totals strip (each category + a grand total) above the By-member table; the **By-member table** renders one column per category (iterated from `CATEGORIES`) + Total + Status; the **drill-down** modal opens with a per-member category breakdown + grand total and tags each session row with its category; the **admin CSV** labels every session/logged row by category. Team Hours has a **By member / Matrix** view toggle. **Matrix** = coach-timesheet member×day grid of **total attendance hours (all categories)** — `attendanceHoursByDate` sums every on-site session per day (the matrix is a physical-presence timesheet; the category split lives in the by-member table + drill-down, reachable by clicking a cell). One row per member incl. zeros, one column per calendar day from season start through today, Sat–Fri weeks each with a subtotal column, a season Total column, a Daily Total footer row, and a sticky member-name column; sortable by Name/Total/each weekly subtotal, default Total desc; needs one season so the 'All Time' tab falls back to the active season. On opening Matrix it **auto-scrolls today's column into view** (`todayThRef` + `scrollIntoView` on the th whose key === today). **Drill-down (both views)**: clicking a member row (By member) opens a modal of their sessions grouped by day for the season; clicking a non-zero matrix cell opens that one member+day's sessions. Each session lists sign-in time, sign-out time (or "— open —"), location (entrance, → exit if different), duration, and category — read straight from the stored `attendance_events` via `sessionsFromEvents`, never recomputed. The admin CSV (By-member view) is every member's full sign in/out history (incl. entrance/exit + category columns) + logged hours; in Matrix view it's the member×day grid with weekly subtotals + Total instead. **Entrance/exit is `attendance_events.location`** — already captured per event (the NFC tag's `?loc` param set by CheckinPage); surfaced via `sessionsFromEvents` (inLoc/outLoc) + `fmtLocation` in hoursUtils. The wide members table scrolls horizontally (`board-table-wrap` is `overflow-x:auto`). My Hours adds an all-time **by-category** breakdown (colored bars), a 6-week trend, recent sessions (each tagged with a non-build category), and a This Week stat. Shared category helpers live in `categories.js`, re-exported from `hoursUtils.js` (`CATEGORIES`/`categoryLabel`/`categoryColor`/`loggedTypeToCategory`/`emptyBreakdown`) alongside `sessionsFromEvents`. **`logged_hours.type` is aligned to the category set** (`supabase/session_integrity.sql` migrated legacy `'volunteering'`→`'volunteer'`; `supabase/categories_reduce_event_kind.sql` then reduced the constraint to the four current categories and re-tagged `fundraising`/`mentoring`→`outreach`); `LogHoursPage` offers all four (manual entry has no NFC tag to derive from, so the user picks; display order Build/Volunteer/Outreach/Competition), chips colored by `categoryColor`. The read-time `loggedTypeToCategory` map still folds any stray `'volunteering'`→`'volunteer'`.
- **Session-integrity tooling** (`supabase/session_integrity.sql`). A "session" is a DERIVED IN/OUT pairing from `attendance_events`, never a stored row. **(1) Forgot-to-sign-out cap** — implemented at derivation time in `hoursUtils.js` (NO synthetic events written): `MAX_SESSION_HOURS` (config constant, default 10h — the one knob; derives `MAX_SESSION_MS`) + `cappedSession(inTime, outTime, {maxMs, eventEnd})` clamp each session's end to `min(eventEnd if tied to a calendar event, IN + maxMs)`. A still-open session UNDER the cap stays live + uncapped; the cap only bites once it goes stale (open past the cap) or a closed pair exceeds it. `cappedSession` is applied in `sessionsFromEvents`, `buildBreakdown`, `computeHoursMs`, `computePendingMs`, and HoursBoard's `attendanceHoursByDate`. Sessions carry a computed `wasCapped` flag (+ `inId`, `manual`) — NOT a DB column; shown as a `capped` badge on My Hours + the Team Hours drill-down. The `eventEnd` clamp is supported by the helper but not yet wired (no consumer currently loads calendar events into the hours path). **(2) Audit trail** — `attendance_audit(event_id, member_id, actor_id, action[insert|edit|delete], reason, old_value jsonb, new_value jsonb, created_at)`; RLS staff-read + member-reads-own, **no client writes** (only the SECURITY DEFINER RPCs insert). Shared infra for (3) and (4). **(3) Admin manual entry / adjustment** — `attendance_events.manual_entry` bool col; staff (`is_staff()`) RPCs `staff_add_manual_session(member,in,out,category,reason)` (inserts a matched IN/OUT pair, `manual_entry=true`, method/location `'manual'`, verified), `staff_edit_event(event,time,category,reason)`, `staff_void_event(event,reason)` (hard-delete, snapshotting old_value first); all require a reason + write the audit via the shared `_apply_event_edit` helper. UI lives in the **HoursBoard drill-down** (staff "+ Manual session" button + per-session Edit/Void via `AdjustPanel`; reloads events after). Off-site non-shop hours stay on `logged_hours` + its existing verification — not rebuilt. **(4) Correction requests** — `session_corrections(member_id, checkin_id, checkout_id, note, proposed_in/out/category, status[pending|approved|rejected], resolution_note, reviewed_by/at)`; RLS member-reads-own + staff-read, writes via RPCs `request_session_correction(...)` (student, validates the referenced events are theirs) and `resolve_session_correction(id, approve, resolution, apply_in/out/category)` (staff; approval applies the edit to the underlying events via `_apply_event_edit` so it audits, idempotent). Student flow: a **Flag** button per recent session on My Hours opens a note+proposed-values modal; status shows in a "Correction requests" card. Staff flow: a **Correction Requests** queue on `/verify-hours` (`VerifyHoursPage`) mirroring the logged-hours verify/reject UX, with editable apply-values before approving. **(5) Logged-hours corrections** (`supabase/logged_hours_corrections.sql`) — the same flow but for **manual `logged_hours` entries** (a different table from the derived-session corrections in (4)): `logged_hours_corrections(member_id, entry_id, note, proposed_type/hours/date, status, resolution_note, reviewed_by/at)`, partial-unique one-pending-per-entry, RLS member-reads-own + staff-read, writes via SECURITY DEFINER `request_logged_hours_correction(entry, note, type, hours, date)` (member; entry must be theirs) and `resolve_logged_hours_correction(id, approve, resolution, apply_type/hours/date)` (staff; approval applies the coalesced edit to the underlying `logged_hours` row, idempotent). Member flow: a **Request correction** button per **verified** entry on `LogHoursPage` opens a reason + proposed-values modal (shows "correction pending" while open). Staff flow: a **Logged-Hours Corrections** queue on `/verify-hours` with editable apply-values.
- **Admin hours management** (`supabase/admin_hours_management.sql` + `src/MemberHoursAdmin.jsx`/`.css`). A **per-member** staff panel embedded at the TOP of `/verify-hours` (`VerifyHoursPage`, same view as the correction queues — not a parallel screen): pick a member, then **(1)** edit (category/hours/date) or delete any of their `logged_hours` rows; **(2)** add a single `attendance_events` row (e.g. a forgotten OUT), edit an event's type/time/category, or delete a stray scan — sessions are NOT stored, they recompute from events via `sessionsFromEvents`/`buildBreakdown`; **(3)** post a labeled **hour adjustment**. All writes go through SECURITY DEFINER RPCs (RLS drops direct cross-user writes): `staff_edit_logged_hours(entry,type,hours,date)`, `staff_delete_logged_hours(entry)`, `staff_add_event(member,type,time,category,reason)`, `staff_set_event(event,type,time,category,reason)` (can flip in↔out, unlike `staff_edit_event`), reusing `staff_void_event` for delete; the attendance RPCs require a reason and write `attendance_audit`. **New `hour_adjustments(member_id, category[build/outreach/volunteer/competition], hours numeric SIGNED, reason, created_by, created_at)`** — the audit-safe way to fix totals without rewriting history; RLS **member-reads-own + staff-read-all** (per spec — so on the all-members Team board, non-staff viewers do NOT see *other* members' adjustments folded in; staff and the member themselves do), writes via `staff_add_hour_adjustment(member,category,hours,reason)`. **Adjustments fold into the category breakdown**: `buildBreakdown` gained a 5th `adjustments` param (signed, attributed to the season of `created_at`, `normAttendanceCategory`'d) — passed per-member in `HoursBoard` (the by-member page) and in `MyHoursPage`. A member sees their adjustments (category, ±hours, reason, date) in an **Hour adjustments** card on My Hours so a correction is never silent. Depends on `attendance_audit` + `is_staff()` from `session_integrity.sql`.
- **Member application** (`supabase/member_applications.sql` + `supabase/member_applications_rls_test.sql` + `src/MemberApplication.jsx`/`.css` + `src/ApplicationsPage.jsx`/`.css` + `src/seasons.js`). The per-season onboarding form that replaces the Google Form, **authenticated only** (no public route, no anon table, no anon insert policy — identity is `auth.uid()`, since every member record descends from `auth.users`). **Renders in the AccessGate slot in `App.jsx`** — after session + `approved`, before the app shell, no NavBar — when the signed-in member has no `member_applications` row for the current season; submitters go straight to the dashboard. **Gated to the member track only**: staff (`mentor/lead/admin`) and parent-only accounts are never asked (they can't truthfully answer pathway / parent contact / build-season commitment, and gating them would lock them out) — one `loadApplicationState` branch in App.jsx. The `/checkin` + `/checkin-volunteer` fast paths are **deliberately let through** so a member mid-session is never blocked from signing out; the gate check also **fails open** if the query errors (deploy-before-migration can't trap anyone). Multi-step form (8 steps → 6 for returning members) with a progress bar + back/next, native HTML5 validation only, all answers in component state, **written once on final submit** so a partial fill never creates a row; no form/validation library added. `member_applications` is one row per member per season (`unique (member_id, season_id)`, `season_id` → `seasons.id` uuid, **"current season" is the existing app rule** — the season spanning today, now shared in `src/seasons.js` (`resolveCurrentSeason`) instead of inlined in HoursBoard). CHECKs: pathway (7 values incl. `Freshman - in rotation`), the 8-value subteam taxonomy, pairwise-distinct ranked choices, Yes/No/Sometimes availability, and both acknowledgments unstorable-as-false. **Contact fields live here and NEVER on profiles** — `profiles` is `select using (true)` to every authenticated member, so parent phones there would publish to the whole team. **RLS**: `insert with check (member_id = auth.uid())`, `select using (member_id = auth.uid() or is_staff())`, and **no update/delete policy for anyone** — corrections route through SECURITY DEFINER per the `admin_member_management.sql` rationale (silent 0-row client writes); the one such path today is `staff_set_discord_confirmed(p_application, p_confirmed)` because `discord_server_confirmed` is staff-set. Grants are `select, insert` only, plus an explicit `revoke all … from anon` (the bootstrap `ALTER DEFAULT PRIVILEGES` grants anon on new public tables, so policy scoping alone is not the anon barrier). `member_applications_rls_test.sql` is a **mutation** test (runs inside a `begin … rollback`, safe on live data): a member can't read another member's row, can't insert with a foreign `member_id`, can't update their own row, and anon has no table privilege — each case fails loudly if the block is missing, with a positive control so a broken-shut policy can't fake a pass. **Three fields write to `profiles`** (already there, already team-visible by design) via the existing direct `profiles update own or admin` policy, same path ProfilePage uses: `nickname` (preferred name), `grad_year` (**integer year, never a grade label** — the dropdown is labeled with this year's grade), `shirt_size`. **Never writes `profiles.subteams`** (staff-set placement) or `profiles.disciplines`. **Prior-experience answers are self-reported and create NO `member_skills` rows** — certification is granted, not claimed; they're display-only to staff. Phones digit-sanitize on change (LoginPage pattern); parent email is `type="email"`. **Returning-member path**: skips prior experience, prefills identity/school/parent/Discord from the most recent prior row, and skips logistics **only when that prior row carries the NOT NULL emergency contact**; subteam interest + commitment are always asked fresh. The meeting schedule is one exported `MEETING_SCHEDULE` constant rendered above the commitment step, which `build_season_acknowledged` references — **the build-season line is still a TODO placeholder**. **Staff view `/applications`** (`ApplicationsPage`, staff-guarded like the other staff pages, in the avatar People group, `APPS` context tag): season picker (defaults to current), search, submitted/returning/not-in-Discord stats, first-choice counts, a row-click full detail modal, the Discord-confirmed toggle, and a **client-side CSV export of the full table** (no Sheets, no Apps Script, no Google API). **`select('*')` on this table no longer works from a client** — see the parent response flow below; both call sites use `APPLICATION_SELECT` from `src/applicationFields.js`.
- **Parent response flow** (`supabase/parent_responses.sql` + `supabase/parent_responses_rls_test.sql` + `supabase/functions/send-parent-request/` + `supabase/functions/parent-response/` + `src/ParentResponse.jsx`/`.css` + `src/applicationFields.js`). Supplementary support info collected from a parent after their student applies. **It GATES NOTHING**: the application is complete without it and no student is ever blocked by a parent who does not answer. Five questions — weekend supervision (saturdays/sundays/either/neither), meal support and competition driving (yes/no/maybe), employer name, and a donate/lend note — plus a separate `employer_contact_consent` opt-in. **Employer name and consent are two controls, never one**: consent is an explicit checkbox defaulting off and is NEVER inferred from a named employer (enforced in the Edge Function too: only a literal `true` counts). **`member_applications.parent_token uuid not null default gen_random_uuid()` unique** is the capability key. **The column lockdown is the load-bearing part**: the student writes their own application row and the table has a select-own policy, so a readable token would let a student answer the form as their own parent. **Note that a plain `revoke select (col)` does NOT do this** — effective column privileges are the UNION of the column ACL and the table ACL, so a column revoke against a role holding table-level SELECT is a no-op. `parent_responses.sql` therefore **drops the table-level SELECT/INSERT grant on `member_applications` and re-grants every other column individually** (generated from the live catalog, so the file is re-runnable). Two consequences: **(a)** a column added to `member_applications` later gets no grant at all until the file is re-run, and **(b)** `select('*')` errors with "permission denied for column parent_token", so both client call sites (MemberApplication prefill, ApplicationsPage list) use the explicit `APPLICATION_SELECT` list in `src/applicationFields.js` — **keep that list in sync with the table**. FLAG: `supabase/calendar_token.sql` hides `profiles.calendar_token` with the naive column revoke and is subject to the same no-op; left alone (different table, different consumers, its own migration). **`parent_responses`** (one row per application, `unique (application_id)`, FK `on delete cascade`; the three enumerated answers are nullable so "did not answer" stays distinct from "no"/"neither"; `submitted_at` + `updated_at` where updated_at is null until an overwrite). **RLS is select-to-staff and nothing else** — no insert/update/delete policy for authenticated or anon, so the Edge Function's service role is the only writer, and a student can never read their own parent's answers. `parent_responses_rls_test.sql` is a rollback-safe mutation test with the same shape as the applications one: non-staff can't select or insert, non-staff can't read `parent_token` even on their own row, anon has no privilege, plus two positive controls (staff CAN read the row; non-staff CAN still read the rest of their own application) so neither a broken-shut policy nor an over-wide revoke can fake a pass. **`send-parent-request`** (JWT-verified, `invite-member` shape, Gmail SMTP via denomailer like `send-approval-email`): the body's `application_id` is a pointer, never a claim — the caller's id comes from the user-scoped client and ownership is decided server-side; a **staff caller may resend for any application** (`is_staff()`), which is the resend path. Plain-text email, no HTML, subject "Your student signed up for FRC Team 5669"; link is `APP_URL` + `/parent/<token>`; skips quietly (200) if the Gmail secrets are unset. **`parent-response`** (**deploy `--no-verify-jwt`** — a capability URL like `calendar-feed`): `fetch` returns only the student's name + any existing response, `submit` upserts on application_id and stamps `updated_at` on an overwrite (the token stays valid so a parent can correct answers); **every enumerated field is validated server-side** against the same vocabulary as the CHECKs; an unknown token returns a **generic 404** that never distinguishes wrong from expired. **Public route `/parent/:token`** is a sibling of `/` and `/login` above the auth-guard wrapper, and is excluded from both the AccessGate and application-gate early returns in App.jsx so a signed-in visitor can't get it swallowed; `ParentResponse.jsx` is an AccessGate-shaped standalone card (loading / form / submitted / invalid / offline) that **never touches a Supabase table**. Wiring: `MemberApplication` submit now does `.select('id').single()` and invokes `send-parent-request` — **a send failure never fails or rolls back the submission**, it surfaces as a note. `ApplicationsPage` surfaces response status at both list levels: a **Parent response table column** (responded + date, or Pending with an inline resend button) and the detail modal (all six fields read-only + its own resend). A **"Pending parent response only" filter** + a stats-strip count give staff the outstanding list without opening each row. **Employer contact consent renders as a colored badge** (`ap-consent-yes`/`ap-consent-no`, shown next to the employer name whenever one is on file) in both places — deliberately loud, since a named employer with consent is a legal sponsorship ask and without it is not, and the two must never be visually confusable. Resend state (`sending`/`sent`/`skipped`/`failed`) is keyed by application id in one `resendStates` map shared by every row's button and the modal's, so concurrent resends on different rows don't collide; a confirmation auto-clears after 4s so the control re-enables itself. The CSV export was extended with the parent response columns (responded, date, and the six fields) — still one row per member, via a `_response` merge done at export time only.
- **Accountability / eligibility tooling** (`supabase/accountability.sql` + `src/accountability.js`). Builds on categories + the integrity work. **(1) Hour goals** — `hour_goals(member_id null=team default, season_id, target_hours, categories text[] null=all categories, updated_by/at)` with partial unique indexes (one team default per season, one override per member/season); RLS members-read-team+own / staff-read-all, writes via SECURITY DEFINER `set_hour_goal(member,season,target,categories[])` / `clear_hour_goal(member,season)`. Effective goal = member override else team default (`effectiveGoal`); progress = sum of the goal's categories from `buildBreakdown` (folds attendance-derived + verified logged hours), via `hoursTowardGoal`/`goalCategoryKeys`. **Category scope choice**: a goal counts toward all categories by default, or a chosen subset stored on the goal row (`categories`); empty = all. **Per-season** (goals are a season minimum). UI: a **progress bar on My Hours** (active-season effective goal); a **Goals / who's-behind view** on Team Hours (`HoursBoard`, third view toggle) sorted by gap-to-goal descending, with staff team-default config + per-member override editor (`GoalEditor`, category toggle chips). **(2) Anomaly flags** (`detectAnomalies(events,{exempt})`, advisory — never auto-deletes): `double_in` (an IN left open before another IN — covers orphan-IN-without-OUT and two-INs-no-OUT, the same pattern under sequential pairing; a trailing open IN is the live session, not flagged), `overlap` (derived sessions whose real intervals intersect), `capped` (sessions clamped by `MAX_SESSION_MS`), `geofence` (an IN with `attendance_events.geo_ok = false` whose member isn't currently `geofence_exempt`). Surfaced in an **Attendance Anomalies** advisory list on `/verify-hours`. Detection is still advisory and never mutates anything, but `capped` and `double_in` cards now carry a **Resolve** button that swaps the list for a `MemberHoursAdmin` scoped to that member (`initialMemberId` + `focusEventIds`), scrolls the flagged `attendance_events` row into view and gives it a ~2s gold highlight. **No resolved/dismissed state is stored** — "Back to anomalies" re-runs `fetchAnomalies()`, so a genuinely fixed item drops off by itself. `overlap` and `geofence` get NO button on purpose: an overlap needs a judgement about which of two sessions is wrong and a geofence flag is about where a member was, so neither resolves by editing one row. When the focused row is an IN with no check-out (a stale capped session, or the orphaned IN of a `double_in`), `EventRow`'s `autoFocusEmpty` opens it in its existing edit mode and adds one field — the missing check-out time, written through the same `staff_add_event` RPC. **That field is ALWAYS EMPTY and is never seeded with the session's capped `effectiveOut`, the auto-close cutoff, or any other computed instant**: a pre-filled plausible time is the fastest way to get a wrong one saved into an hours ledger, so staff type what they actually know. The capped value stays a read-only display elsewhere. **What that rule costs is precision nobody has** — an exact minute for a session months old — so the block offers two ways out that do not require one, both additive to `autoFocusEmpty` and neither touching the normal edit path. **(a) A half-hour stepper**: `-30m`/`+30m` build the check-out as an offset from the check-in with a live "Session length: 1h 30m — ending 9:30 AM" readout (the ending date is named too when it crosses midnight). `outTime` stays the SINGLE source of truth, which is what keeps the stepper and the datetime-local fallback from fighting: a step reads whatever is in the field right now, so typing a time becomes the baseline the next click adjusts from, and no separate offset state can drift out of sync. The low end clamps at the check-in — stepping down to or past it CLEARS the field, which is both the no-zero-length-session rule and how the nothing-entered starting state is restored; the high end is deliberately unclamped. The starting offset is 0 and the field still mounts at `''`. **(b) A one-click void** (`Cancel — don't count this session`, `mha-btn-danger`) for when no honest check-out exists at all, on the existing `staff_void_event` RPC the row's own Delete already calls. Its FIRST click fills the shared reason field with a default sentence and does NOT void — so the required-reason field is the confirmation step exactly as it already is for Delete, rather than a second dialog, and a misclick is harmless. That default is a static sentence about why staff voided the row, never a claim about the student's session, which is why it may be pre-filled where a timestamp may not. **RESOLVING NOW HAPPENS IN THE ANOMALY LIST ITSELF for the common case, and the eventIds shape is what decides which case that is** — `double_in` always (eventIds[0] is the orphaned IN), and `capped` only when `eventIds.length === 1`, which is `sessionsFromEvents` handing back a still-OPEN session (`outId` null) that has run past `MAX_SESSION_MS`: the true missed-checkout case, where `a.at` is the check-in. **`capped` with length 2 means a REAL check-out event exists** and the pair merely exceeds the cap — adding a second OUT would not fix that, so it is an EDIT and keeps the old route into `MemberHoursAdmin`, labelled `Open member events →` with a note saying why. Each inline card expands in place (no panel swap, no member search, no full-ledger fetch, no scroll-and-highlight) and carries **one-click duration presets (30m/1h/1h30m/2h/2h30m/3h) over the ±30m fine adjust**, the shared readout, a reason field, `Add check-out` and a two-click `Don't count this session`. On success the item is spliced out of the local array and the mentor's next click is the next card — there is no Back. **BULK**: a checkbox per inline-resolvable card, a select-all scoped to only those, then `Void N selected` or `Set N selected to <duration>` — the latter applied as a **LENGTH from each item's own check-in**, never one shared clock time. Bulk loops the SAME single-event RPCs sequentially (no batched endpoint, and deliberately not `Promise.all`: each call writes an `attendance_audit` row, and a burst is both the shape most likely to trip a rate limit and the one whose partial failure is hardest to report honestly); a progress counter makes the few seconds visible. **A failed item is never counted as done** — it stays in the list with its own inline error and the result line says so. **The bulk result line renders OUTSIDE the bulk bar**, because a successful bulk action empties the selection and unmounts the bar, so a note inside it vanished at the exact moment it had something to say (found by running it, not by reading it). **`src/hoursResolve.js` is the shared pure module** both surfaces call — `stepMinutes`/`setPreset`/`endInstantMs`/`minutesFromInstant`/`resolveReadout`/`DURATION_PRESETS`/`STEP_MINUTES` — no React, no Supabase: it computes values and the call site owns the state and the RPCs. **ITS CANONICAL MODEL IS AN OFFSET IN MINUTES WITH `null` MEANING NOTHING ENTERED**, which is the no-pre-filled-timestamp rule expressed in the type — there is no zero-ish starting instant to render, so a caller cannot accidentally show one, and `endInstantMs` is the single place an offset becomes a timestamp. A preset REPLACES the offset rather than chaining onto it. The per-member `EventRow` keeps its datetime-local field as ITS source of truth and re-derives the offset each render, so that surface is unchanged in behaviour and gained only the presets. **The reason text is shared across every resolve action for one page load** (single and bulk alike) so a mentor clearing a backlog types it once; it carries no time and no claim about a student, and nothing persists it. `unclosedInIds()` in MemberHoursAdmin does that pairing itself rather than reading `sessionsFromEvents`, which overwrites an open IN when a second IN arrives and so produces no session at all for the orphan. Anything else is still fixed via Team Hours edit/void/manual. **`attendance_events.geo_ok`** (bool, nullable) is set at check-in: `true` = geofence verified, `false` = exempt member skipped it, `null` = legacy / staff-bypass / check-out. **(3) Attendance vs hours** — `daysPresent(events,{since,until})` = distinct calendar days with an IN; shown **separately** from clocked hours: a "Days present" stat on My Hours' goal card, a **Days** column on the Team Hours by-member table, and a Days column in the Goals view. Meeting attendance is scoped as days-with-a-check-in (decoupled from the schedule; tying to specific `events` rows is a possible refinement).
- **Reporting / export tooling** (`/reports`, `ReportsPage.jsx` + pure `src/reporting.js`; staff-only, in the avatar STAFF menu). Reads **both hour sources** — attendance_events-derived sessions (capped, via `sessionsFromEvents`) and verified `logged_hours` — and flattens them into export rows carrying the `wasCapped` (derived), `manual_entry`, and `review` (pending/voided session_reviews) flags. **No schema change**: hours are tied to calendar events by **date/time window match**, not an explicit link column — `buildRows`/`matchEvent` attribute an attendance session to the earliest event whose `[starts_at, ends_at]` window it overlaps, and a logged entry to the earliest event whose LA-local date span contains its date (the check-in fast paths don't know their event, so a stored link would drift). Three tabs: **Per-event rollups** (`rollupByEvent` — per-event category split + total + distinct member count, e.g. per competition weekend / build day); **Exports** (filter by student / date range / category / event; per-student or team-wide; CSV download via `rowsToCsv` + print-to-PDF via `exportHtml`; flags as columns); **Service-hour letters** (`letterData`/`letterHtml` — per student/volunteer for a date range: name, total, category breakdown, range, team identifier "Techmen — FRC Team 5669 / Don Bosco Technical Institute", itemized appendix + preparer + mentor signature line for verifiability; service categories default to volunteer/outreach, adjustable). **PDF = a print window** (`window.open` + embedded print CSS + `window.print()`) so it's dependency-free, offline, and lets the browser "Save as PDF" — no PDF lib added. Letters/rollups use official hours (exclude review-flagged); the raw export includes everything with the flag columns.
- **Subteam vocabulary** (`src/subteams.js` + `supabase/subteams_vocabulary.sql` + `supabase/subteams_tasks_constraint.sql` + `supabase/subteams_add_strategy_management.sql`). ONE canonical list, imported by ProfilePage (the profile chips), JobsPage (the job-form select), and MemberApplication (the three ranked choices) — no file keeps its own copy. **12 values, byte-for-byte: `Mechanical`, `Electrical`, `Programming`, `CAD`, `Fabrication`, `Media`, `Business/Outreach`, `Drive Team`, `Robot Construction`, `Field & Pit`, `Strategy and Scouting`, `Management`.** The last two were APPENDED (2026-08-23, `supabase/subteams_add_strategy_management.sql`) for the nine subteams named at the first team meeting — purely additive, no existing value renamed and no row rewritten. **Widening the vocabulary means widening BOTH CHECKs in one pass** (`member_applications_subteam_values_chk` and `tasks_subteam_values_chk`), and the two earlier migration files were extended to match so re-running one cannot silently revert the addition. `member_applications.subteam_first/second/third` are DB-constrained (`member_applications_subteam_values_chk`, re-pinned by `subteams_vocabulary.sql`, **NOT VALID when legacy application rows exist** — historical answers are left unrewritten, no lossless mapping). **`tasks.subteam` is now DB-constrained too** (`tasks_subteam_values_chk`, nullable-aware, same NOT-VALID-if-non-canonical-rows-exist pattern, added by `subteams_tasks_constraint.sql`); **JobsPage's job form is a real `<select>`** restricted to the canonical list — editing a job that already holds a non-canonical value keeps that one value selectable+selected (so an unrelated edit doesn't silently retag it), new jobs only offer canonical values. **`profiles.subteams` (text[]) is DELIBERATELY left unconstrained** — stores these literals with **no DB constraint**, so renaming/recasing/repunctuating an entry silently orphans live rows with no error; `subteams_tasks_constraint.sql` audits its distinct values (report only, no write) for staff to review by hand — append new values, never edit one in place without a data migration. **Every grouping/sorting consumer is data-driven, not list-driven** — `presence.js` `groupBySubteam` (A→Z + UNASSIGNED last, shared by PresenceBoard + ParentHomePage), RosterPage's subteam sort, JobsPage's group ordering, and `readiness_summary`'s project_staffing all read whatever value is stored, so new values need no per-consumer change and there is no color/icon/order map keyed to the list.
- Profile customization (subteam, nickname, bio, shirt_size, avatar_url, grad_year). Multi-select subteams for crossover members; the subteam list comes from `src/subteams.js` (see above).
- Admin roster (`supabase/admin_member_management.sql`): manage roles (student, mentor, lead, admin, parent) and status (active, inactive, alumni), gated by has_role('admin'). **Role grant/revoke goes through the `admin_set_member_role(p_member, p_role, p_grant)` SECURITY DEFINER RPC** — a direct client delete on member_roles silently affects 0 rows (no error) when the write policy doesn't match, so edits looked applied but never persisted; the RPC enforces admin server-side and raises loudly. Roster has text search (name/nickname/email) + sort (name/subteam/role/hours). **Nickname is the display name across the roster**, full_name fallback; `admin_get_members()` now returns nickname. **`admin_delete_member(p_member)`** hard-deletes a member behind a type-the-full-name confirm dialog: it nulls actor references (reviewer/creator/certifier/added_by/assigned_by/overridden_by cols) then deletes the profile (cascades attendance, hours, skills, task_claims, signups, study, push, guardian links, position assignments, roles) and the auth user (session_reviews + auth.* cascade) — no orphans; can't delete yourself.
- Access-request system + `parent` role (`supabase/access_requests.sql`). Non-allowed-email sign-ins hit a request form (replaces the old AccessGate dead-end) that writes an `access_requests` row; staff review at `/access-requests` and Approve (with assigned role student/mentor/parent) or Deny via SECURITY DEFINER RPCs. Approve whitelists the email in `approved_emails`; `claim_profile()` approval order is domain → approved_emails (grants the stored role) → already-approved (still defaults to student). Approval sends a courtesy email via the `send-approval-email` Edge Function (Gmail SMTP via denomailer; secrets `GMAIL_USER` + `GMAIL_APP_PASSWORD`, optional `EMAIL_FROM`/`APP_URL`) — approval never fails if those are unset. boscotech.edu still auto-approves as student.
- Staff invite flow (parent-friendly, additive to the request flow): the `/access-requests` page has an "Invite someone" panel that calls the `invite-member` Edge Function — it whitelists the email in `approved_emails` with a role (default parent) and sends Supabase's built-in one-tap invite link via the project SMTP, so the invitee clicks through and lands in already approved (no request/wait). Falls back gracefully if they already have an account. Requires Supabase Auth **Custom SMTP** configured and the app URL in Auth → URL Configuration (Site/redirect URLs).
- Login screen (`LoginPage.jsx`) routes by audience: "Continue with school account" (Google, org-internal `@boscotech.edu`) for students/mentors; an email sign-in-code path labeled "parents & guests" so non-org users don't hit Google's org_internal block. The Google OAuth consent screen is Internal by design — external users must use the email path.
- Scheduling module (`supabase/events.sql`). `events(title, kind, starts_at, ends_at, location, notes, ...)` — readable by all authenticated, writable by `is_staff()` only (plain staff-write policy, no RPCs). **Event kinds**: `build`/`meeting`/`competition`/`potluck`/`outreach`/`volunteering`/`training`/`other` — defined in `SchedulePage.jsx` `KINDS` + the `events_kind_check` constraint (`volunteering` added via `supabase/categories_reduce_event_kind.sql`, `training` via `supabase/events_kind_training.sql`; `events.sql`'s inline CHECK is re-pinned to the full list so a fresh install matches). Each is styled by a `.sch-kind-<kind>` class, surfaced as a text label in the per-event rollups, and carries a `KIND_COLORS`/`KIND_LABELS` entry in `supabase/functions/discord-calendar/lib/render.js` (`training` → 'Training'; an unknown kind falls back to `other`/'Event' rather than throwing). **The kind vocabulary is duplicated across those four sites with nothing enforcing agreement — adding a kind means touching all of them.** The `calendar-feed` function and `ReportsPage` pass `kind` through generically and need no change per kind. **No event kind has an emoji anywhere** — the schedule pill is the uppercased raw kind string, Discord uses a colour + text footer. Only `build` opens the shop (`SHOP_OPEN_KINDS`). `/schedule` is an agenda list grouped by day (all roles view; staff create/edit/delete via a form; past events behind a toggle). **Shop open/closed rule:** the shop is "open" only during today's `kind='build'` event windows (other kinds do not open the shop); status is "Open until END" inside a window, "Closed, opens START" before the first, else "Closed today", with a live "N checked in" override from `presence.js`. The shop-status derivation + the dashboard glance card live in one shared module (`shopStatus.js` + `GlanceCard.jsx`) imported by both HomePage and ParentHomePage; the `/checkin` fast path deliberately imports none of it. Event times render in America/Los_Angeles via `toLocaleTimeString`. **Calendar Phase 1** (SchedulePage): in-memory Agenda/Month/Week view toggle (Agenda is the default), a "My events" filter (events RSVP'd going + every shop-open `SHOP_OPEN_KINDS` session) across all three views, and form conflict signaling (non-blocking warning naming time-overlapping events, excluding the edited one) + FULL capacity badges on Month chips and event heads. Month/Week use LA day-key (`dayKey`) bucketing with UTC-based date math (`addDays`/`addMonths`) so they never drift on DST; event rows are a shared `renderEvent`/`renderDayGroup` so RSVP behavior is identical everywhere. **Calendar Phase 2 — iCalendar subscription feed** (`supabase/calendar_token.sql` + `supabase/functions/calendar-feed`): `profiles.calendar_token uuid` (unique, auto-defaulted on every row) is a capability key. The token column is **revoked from client select/update** (profiles is broadly authenticated-readable and RLS is row-scoped, so a `profiles(calendar_token)` embed would otherwise leak everyone's token); a member reads/rotates only their OWN token via SECURITY DEFINER RPCs `get_calendar_token()` / `rotate_calendar_token()`. The `calendar-feed` Edge Function (self-contained, **deploy `--no-verify-jwt`** — it's a no-JWT capability URL, authenticates by `?token=`) serves a hand-rolled RFC 5545 `.ics` (CRLF, 75-octet folding, text escaping, UTC `DTSTART`/`DTEND`, per-event `UID`/`SEQUENCE`/`LAST-MODIFIED` from `updated_at`; no RRULE since series rows are pre-expanded); `?scope=mine` (default) mirrors the My-events filter, `?scope=all` is everything, both from 30 days ago forward; uses only `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY`. ProfilePage has a "Calendar subscription" section (webcal one-tap link, https add-by-URL field + Copy, mine/all scope toggle, Regenerate).
- Event RSVPs / signups (`supabase/event_signups.sql`). Additive columns on `events`: `rsvp_enabled` (bool), `capacity` (int null). New `event_signups(event_id, member_id, response[going/maybe/declined], item, …)`, PK (event_id, member_id) — one upserted row per member per event. RLS: readable by all authenticated; insert/update/delete gated to `member_id = auth.uid() OR is_staff()` — plain own-row writes, **no RPCs** (reserve the RPC pattern for cert-gated/privileged transitions). Any approved member (student/parent/staff) can RSVP. The RSVP control + attendee list render inline on `/schedule` **only when `rsvp_enabled`** (non-RSVP events are visually unchanged), reusing the existing schedule UI. Capacity is **informational** (shows "N of CAP", soft-warns when full) — it does NOT hard-block; strict-limited slots would need a row-locking sign-up RPC (follow-up).
- Parent dashboard + guardian links (`supabase/guardian_links.sql`). `guardian_links(parent_id, student_id, created_by)` links a parent to students; staff manage links on RosterPage (parent-role members get a Students section) via `link_guardian`/`unlink_guardian` SECURITY DEFINER RPCs. RLS: readable by staff or the parent themselves, writable by staff only; a `lh parent select` policy lets a parent read their linked students' `logged_hours` (other tables were already authenticated-readable). `/dashboard` renders `ParentHomePage` (per-student live status/hours/certs + read-only team glance) for `(hasRole('parent') && !isStaff)`; NavBar shows only Dashboard + Team Hours for that case. Shared hours math lives in `hoursUtils.js` (`computeHoursMs`/`fmtDuration`). **Self-service parent linking** (`supabase/parent_link_requests.sql`): a parent (the parent-view dashboard) searches the roster and submits a link request via `request_parent_link(p_student, p_note)` (own pending row, idempotent — reuses an existing pending request); staff review pending requests on `/access-requests` (a "Parent link requests" section under the access queue) and `approve_parent_link`/`deny_parent_link`. **Approve is idempotent**: the guardian_links insert is `on conflict do nothing` and the status update always runs, so one click reliably links (the old failure mode was a PK violation aborting the txn so status never moved → "approve twice, still failed"). `parent_link_requests` RLS: staff read all / parent reads own; all writes via the SECURITY DEFINER RPCs. The NavBar staff pending badge counts access_requests + parent_link_requests.
- Skills and certifications: catalog, member view, certify screen with audit trail. **`/skills` is role-split**: staff get `SkillsCatalog` (management); non-staff get `MemberSkillsHome` (personal dashboard — own certs/in-progress via `MemberSkillsPanel` + a cert-request form, with a toggle to view the team coverage matrix read-only via `CoverageMatrix canView`). Previously students saw a "Staff access required" dead-end. **Member cert requests** (`supabase/cert_requests.sql`, mirrors parent_link_requests): a member submits `request_cert(p_skill, p_note)` (own pending row, one-pending unique guard, idempotent); staff review on `/access-requests` ("Cert requests" section) and `approve_cert_request`/`deny_cert_request`. **Approve is idempotent + race-safe** (row lock + member_skills upsert to 'certified' + status update always runs), so one click certifies the member and they appear on the coverage matrix. RLS: staff read all / member reads own; all writes via SECURITY DEFINER RPCs. NavBar staff pending badge now counts access + parent-link + cert requests.
- Hours types / seasons table (Offseason 2026 ends 2027-01-06, Biocore 2027 starts 2027-01-07). Logged hours (volunteering, outreach, competition) with mentor verification.
- **Discord server provisioning** (`scripts/discord/SERVER_SPEC.md` + `scripts/discord/provision.js` + `scripts/discord/README.md`). Ops tooling, NOT app code — it never imports from `src/` and ships nothing to the browser (`@discordjs/rest` + `discord-api-types` are devDependencies). Takes the team Discord guild from empty to fully configured in one run: guild settings → the two Community prerequisite channels → Community enablement (`PATCH /guilds/{id}` adding `COMMUNITY` to `features` with `rules_channel_id`/`public_updates_channel_id`) → roles → `@everyone` deny → categories → channels → AutoMod → onboarding. **`SERVER_SPEC.md` is the source of truth and is PARSED at runtime** — the role table, every channel permission matrix, the safety bullets and the onboarding questions — so a spec edit changes the plan with no second copy to sync. Only three vocabularies are hardcoded (colour word→hex, permission phrase→bitfield, `V`/`S`/`-`→overwrite) and each throws on an unknown token rather than dropping it. Dry run is the default and is read-only; `--apply` is required to write; `--offline` plans against a simulated empty guild with no token (and `--dump-state`/`--state` replay a plan over its own result to prove idempotency). **Idempotent, matched by name; never deletes** — anything in the guild but not in the spec is reported as UNMANAGED. Rate limits are honoured off the returned `retry_after`, never a fixed delay. Env: `DISCORD_BOT_TOKEN` + `DISCORD_GUILD_ID` from `process.env` or `.env.local` via `node --env-file`; npm scripts `discord:plan`/`discord:apply`/`discord:offline`. Unresolved spec ambiguities (what a roleless member sees, Student Lead's matrix row, `V`-and-reactions, hoist/mentionable, AutoMod exemptions) are listed in the README rather than silently decided.
- **Discord calendar posting** (`supabase/functions/discord-calendar/` — `index.ts` + `lib/{discord,render,calendarSync}.js` + `README.md`; `supabase/discord_calendar.sql`; `scripts/discord/calendar-sync.test.mjs`). Hourly pg_cron → pg_net → Edge Function posting `public.events` into the team Discord: one embed per upcoming event in **#calendar** (title, start/end in America/Los_Angeles, location, notes), plus a reminder in **#announcements** at 2h. **`reminder_24h` was RETIRED 2026-08-26** and is the one entry commented out of `REMINDERS` in `lib/calendarSync.js`; nothing else was removed. The Sunday week-ahead digest already says what is happening tomorrow, so the 24h reminder was the one duplicated post that also pinged a role, while the 2h reminder is same-day and nothing else covers it. Restoring that single array entry brings it back: the `reminder_24h` rendering path, its existing `discord_calendar_posts` dedupe rows and its `discord_calendar_log` rows are all deliberately intact, so already-posted 24h messages are still edited on a change and still struck through on a cancellation (the cancellation sweep walks the tracking table, never the `REMINDERS` array). `SHORTEST_LEAD_MS` is derived from `REMINDERS` rather than indexed into it, so adding or removing a lead cannot leave the supersede guard reading the wrong element. **Invocation auth matches send-push / cron-notify** — a shared secret in a custom header (`x-cron-secret` vs the `DISCORD_CRON_SECRET` function secret), **deploy `--no-verify-jwt`**, with `edge_base_url` + `hook_secret` in a private config table read only by a SECURITY DEFINER invoke function. That config is **`private.discord_calendar_config`, deliberately NOT `private.push_config`**: filling in push_config activates the push triggers/crons, and that deploy is still on hold, so sharing it would chain one release to the other. Null `edge_base_url` = the cron no-ops, which is the pause switch. **This is the one function in `supabase/functions/` that is NOT single-file** and so cannot be deployed from the Dashboard editor (`npx supabase functions deploy discord-calendar --no-verify-jwt`): `lib/` is the same code `scripts/discord/calendar-sync.test.mjs` imports under Node, and inlining it would mean two copies of the no-double-post logic. The engine is written to the **Deno∩Node intersection** — plain ESM, explicit file extensions on relative imports, nothing imported beyond `node:crypto` — so one copy runs in both; keep it that way. **FAILURE ISOLATION (2026-08-29): the cancellation sweep is NON-FATAL and read-only queries carry a bounded auth retry.** Production logged 9 fatal aborts in 168 hourly runs, all `loading cancellation candidates: JWT issued at future`, run_start 168 vs run_end 159. The sweep holds the run's FIRST PostgREST call, so a transient service-role JWT rejection (Edge-runtime clock ahead of the database, so `iat` looks future-dated) aborted the whole run before a single post was considered. The sweep only EDITS already-posted messages, so its failure is now caught at the call site, logged as `error` with `fatal: false` and `phase: 'cancellation_sweep'` (same detail shape, plus `summary.sweep_error`), and the run continues to posting and still writes `run_end`; the Edge Function still returns 200 because `ok` keys off `summary.fatal`. `readWithRetry` retries at most twice (250ms, 750ms, `config.sleep` injectable) and ONLY on an auth-shaped Supabase error (PGRST301/PGRST302, or `jwt`/`issued at`/`invalid claim` in message+hint+details), never on a `DiscordError` and never on a permission error like 42501 which is not transient. **It wraps SELECTs and nothing else** — that is the whole duplicate-post argument: a read cannot reserve a tracking row and cannot call Discord, so no number of attempts touches `unique (event_id, post_kind)`; the reserve-then-send path in `upsertPost` is deliberately NOT wrapped and must never be. **Widening the retry to all four reads was necessary, not incidental**: making only the sweep non-fatal would have moved the same fatal abort onto `loading events`, the next PostgREST call. **Still fatal, deliberately**: `resolveGuild` and a missing #calendar/#announcements channel (the channel id is written into the reserved tracking row, so continuing would burn a dedupe key against the wrong channel and permanently block the real post), `loading events` (the run's whole input), and `loading tracking rows` — that map IS the answer to what has already been posted, so proceeding without it sends every posted event down the CREATE branch; the unique constraint still blocks a duplicate message, but the run would stop editing changed messages and fill the log with false already_reserved skips, which is the duplicate-shaped failure the rule is about. **NEW log action `reminder_missed`** closes the silent-loss gap: when an event has started and no reminder row exists, the slot was closed with a `superseded` row indistinguishable from the routine planned close, so nobody-was-told looked identical to we-chose-not-to. `reminder_missed` is now logged first, carrying event_id, post_kind, lead, event_starts_at and minutes_late, exactly once per (event, kind) because the supersede row makes every later run take the `existing` branch. **No late reminder is posted and no Discord call is made** — it is a log row only. `auth_retry` is the other new action. Both, plus the `fatal` true/false split on `error`, are documented in `supabase/discord_calendar.sql`; `discord_calendar_log.action` is open text with no CHECK, so **neither needed a migration**. Test suite is 19 (was 13). **Report-only is the default** — only the literal `DISCORD_CALENDAR_MODE=live` enables writes, and report mode logs the intended posts WITHOUT reserving tracking rows, so dry runs never burn a dedupe key. **The no-double-post guarantee is a DB constraint, not app logic**: `discord_calendar_posts` is `unique (event_id, post_kind)` and every post is **reserve-then-send** (insert `status='pending'` → POST → update to `posted` + `message_id`), so overlapping runs race on the constraint and the loser posts nothing. A definitive 4xx releases the reservation (no message exists); an **ambiguous** failure (network drop, exhausted 429/5xx) deliberately KEEPS it as `pending` and never auto-retries — a visible stale row beats a duplicate ping to sixty people. **Changed event → the rendered payload hashes differently → the stored `message_id` is EDITED, never reposted.** **`discord_calendar_posts` deliberately has NO foreign key to `events`**: `public.events` has no `cancelled` column (staff cancel by DELETING the row via SchedulePage), so a cascade would destroy the evidence at the exact moment it is needed; the row plus its `event_snapshot` jsonb outlive the delete, and the sweep (`status='posted'` + `event_ends_at >= now()` + event id no longer present) edits the message to struck-through **CANCELLED** and keeps it. Deleted-after-it-ended is housekeeping, not a cancellation, and is left alone. `10008 Unknown Message` → row marked `archived`, never resurrected. **`events` has no subteam column either**, so "the event names a subteam" is taken literally. **`render.js` does NOT read `src/subteams.js`** (an earlier revision of this file said it did, which is wrong, and is what kept sending sessions in circles): it word-matches its OWN curated `SUBTEAM_ROLE_ALIASES` map over title+notes and emits SERVER_SPEC §3 Discord role names. That map is **11 alias entries resolving to 7 distinct roles** (Mechanical, Electrical, Programming, CAD, Business/Media, Scouting, Drive Team); several aliases fold onto one role (`business/outreach`, `business/media`, `business & media` and `media` all resolve to Business/Media) and two role names deliberately differ from the app's (`Business/Outreach` the subteam vs `Business/Media` the role, `Strategy and Scouting` the subteam vs `Scouting` the role). Fabrication/Robot Construction/Field & Pit have no Discord counterpart and intentionally ping nobody. `allowed_mentions` is pinned to the resolved role IDs with `parse: []`, so `@everyone` can never fire. **No snowflake is stored anywhere** — channels and roles resolve by name every run. 429s honoured off `retry_after` (body first, `Retry-After` header fallback), plus proactive parking on `x-ratelimit-remaining: 0`; every action (including `plan_*`, `skipped`, `rate_limited`, `superseded`) lands in `discord_calendar_log` keyed by `run_id`. Both tables are RLS staff-read / no-client-write (service role is the only writer). **Bot token is a function secret** — not `VITE_`-prefixed and nothing under `supabase/functions/` is in the Vite build. `npm run discord:calendar:test` runs 13 in-memory tests (fake supabase incl. the unique constraint + fake Discord) covering dedupe, edit-not-repost, cancellation, reminder windows, the 4xx-vs-ambiguous reservation rule, and the 24h retirement (an event inside 24h opens no `reminder_24h` row, and the surviving 2h reminder still fires on the first tick inside its own window and exactly once).
- **`supabase/config.toml` pins `verify_jwt = false` for every pg_cron-invoked and capability-URL Edge Function.** `cron-notify`, `send-push`, and `discord-calendar` are invoked by pg_cron/pg_net with a shared secret in a custom header (`x-push-secret`/`x-cron-secret`), never a user JWT; `calendar-feed` and `parent-response` are capability URLs authenticated by an unguessable token in the query string, never a JWT. All five need `--no-verify-jwt` at deploy or the API gateway rejects every call before the function body runs. That flag used to live only in code comments and deploy-command reminders — easy to drop on a deploy someone runs without `--no-verify-jwt` — so `config.toml` now makes the CLI apply it automatically per function. `invite-member`, `send-approval-email`, and `send-parent-request` are invoked with the caller's own signed-in JWT (staff-authorization or ownership checks happen inside the function against that JWT) and are deliberately NOT listed — they keep default JWT verification on.
- **THE TWO DISCORD RENDERERS DISAGREE ABOUT SUBTEAMS ON PURPOSE, AND ARE NOT BEING RECONCILED.** `supabase/functions/discord-calendar/lib/render.js` matches a **curated alias map** (`SUBTEAM_ROLE_ALIASES`, 11 aliases to 7 roles) over **title + notes** and emits **Discord role names**. `api/_lib/weekAhead.js` matches the **canonical 12-value `src/subteams.js` vocabulary** over the **title only** (an explicit `Subteams: A, B` line in title or notes overrides it outright) and emits **app subteam names**. **The role set is smaller than the subteam set on purpose.** The reason is what each output DOES. The calendar reminder's output PINGS a role, and only a role that actually exists in the guild can be pinged, so its list is bounded by SERVER_SPEC §3 and a subteam with no role must resolve to nobody rather than to a broken mention. The week-ahead's output is TEXT A STUDENT READS, sitting beside the very same subteam names in frc-app, so it uses the canonical vocabulary and would be wrong to show a student a Discord-only name like Business/Media. The scope difference follows from the same split: the curated alias list contains no common English word, so title+notes is safe for it, whereas the canonical vocabulary contains `Management`, `Media`, `CAD` and `Fabrication`, and running those over notes prose ("Electrical runs wire management") attributed sessions to Management, which is simply false. **Do not try to make the two lists match.** Widening the alias map is correct only once the corresponding Discord role actually exists.

- **Week-ahead schedule post** (`api/discord-week-ahead.js` + `api/_lib/weekAhead.js`; `vercel.json` `crons`). One Discord post every Sunday evening naming the coming Mon-Sun week, so routine schedule information stops arriving as a same-day server-wide ping. **This re-establishes `api/`**, which had been deleted when the Discord calendar poster was ported off Vercel to a Supabase Edge Function; `vercel.json`'s SPA rewrite is narrowed to `/((?!api/).*)` again so it cannot swallow the function. **Cadence is enforced IN the function, not in the cron expression**: Vercel Hobby crons are daily-granularity (a day-of-week field is not honoured), so the cron fires daily at `0 1 * * *` UTC (which is 5 PM or 6 PM the PREVIOUS LA day either side of DST, so it always lands on that day's evening) and the handler no-ops with a 200 + reason on any day that is not Sunday in America/Los_Angeles. Auth is `Authorization: Bearer <CRON_SECRET>`, compared as sha256 digests so the check is constant-time and length-independent; anything else is a 401. **`allowed_mentions: { parse: [] }` is the hard guarantee.** The post cannot resolve `@everyone`, `@here`, a role or a user even if one of those strings is in an event title (verified by seeding exactly that). Reads `public.events` by OVERLAP (`starts_at < windowEnd and ends_at >= windowStart`, so a competition that opened the Sunday before still appears) and `public.tasks` where `status = 'open'` and `due_date` is inside the window. **Server credential is `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY`**, matching every other server-side reader here (calendar-feed, cron-notify, discord-calendar, parent-response): `events` RLS is `select to authenticated using (true)` with NO anon policy, so the anon key cannot read the schedule at all and a cron has no user session to be authenticated as. **An empty window still posts**, stating plainly that nothing is scheduled, because silence is indistinguishable from the job having failed. `?dry=1` renders and returns the body without posting and without needing `DISCORD_WEBHOOK_URL`; `?at=<ISO>` forces the evaluated instant and is **gated to the dry path only** so it can never shift which week goes out for real. **No secret is ever logged, echoed or returned.** A `scrub()` helper strips any occurrence of `CRON_SECRET`/`DISCORD_WEBHOOK_URL`/`SUPABASE_SERVICE_ROLE_KEY` out of anything bound for a response or the console (proved at runtime against a stand-in webhook that echoes all three back in its error body: 0 occurrences survive); the two "is not set" errors name the VARIABLE, never a value. **`events` has NO subteam column**, so subteam attribution reads the text staff already write, in two signals: an explicit `Subteams: A, B` line in title or notes wins outright, otherwise a word-bounded vocabulary match against the **TITLE ONLY**. Title-only is deliberate and was found by running the dry run: the nearest prior art (`discord-calendar/lib/render.js`) matches title+notes but against a hand-curated alias list containing no common English word, whereas this matches the full 12-value `src/subteams.js` vocabulary, which contains `Management`, `Media`, `Fabrication` and `CAD`. Over notes prose reading "Electrical runs wire management" it attributed the session to Management, which is false. A title names what a session IS; notes are prose. Imports `SUBTEAMS` from `src/subteams.js` rather than copying it. Generated copy is pure ASCII with no em dashes.

### Roadmap (planned modules)

- Custom scouting app and system.
- Calculators and reference tables for robot design.
- Automated FRC learning environment so students can build skills and take on more responsibility during the season. Offseason priority.
- Job and task list with assignment to members.

This list will grow. When building a new module, confirm the members table and roles already carry it before adding new foundation.

## Commands

```bash
npm run dev                          # dev server
npm run build                        # the build, and this repo's only type gate (see below)
npm test                             # the vitest suite in tests/
npm run test:watch                   # the same, watching
npm run ds:audit                     # the design system's 30 static checks
npm run discord:calendar:test        # the Discord calendar engine's 19-case suite
npm run history:verify               # docs/history/ is still a lossless split
npm run history:index                # print the history indexes (never committed)
npm run verify:browser               # the visual pass: real Chromium against /_ds
npm run verify:browser -- --selftest # 36 negative controls for the checks themselves
npm run verify:browser -- --probe    # what this machine's browser can actually do
```

Local-only, deliberately not in CI:

```bash
npm run ds:audit:controls            # breaks each ds:audit check in turn; REFUSES a dirty tree
npm run ds:dev                       # boot the /_ds specimen at :5175 (vite --mode dsspec)
npm run ds:capture                   # render every sheet pattern to PNG
npm run discord:plan | :apply | :offline   # Discord server provisioning
python3 /tmp/status.py --repo FRC-Team-5669-Techmen/frc-app   # read-only status, see below
```

**There is no type check and no linter in this repo.** No `tsconfig.json`, no ESLint config, plain `.jsx` throughout. **`npm run build` is the type gate**, in the only sense one exists here: it fails on a syntax error, an unresolved import, or a module that throws at load. A `.d.ts` sweep does run, but only over the design system's own declaration files and only inside `ds:audit`. Do not report "typecheck passed" — say the build passed.

**`ds:capture` does not run in this container, and that is not a fault to fix here.** It launches with `channel: 'chrome'`, the machine's installed Google Chrome, and fails with `Chromium distribution 'chrome' is not found at /opt/google/chrome/chrome` — only the playwright build (`/opt/pw-browsers/chromium-1194/`) is present. `verify:browser` reaches the same browser through an explicit `executablePath`, which is why it works. Measured 2026-09-02.

**The status tool is fetched, not committed here.** It lives in the sibling repo and is read-only — it clones into its own temp directory, writes nothing, and collides with no open session:

```bash
curl -sfL https://raw.githubusercontent.com/pina-hash/idea-app/main/tools/idea-status.py -o /tmp/status.py
python3 /tmp/status.py --repo FRC-Team-5669-Techmen/frc-app
```

It reports standing `claude/**` branches, `main` against `integration` in both directions, and a migrations section. **That section will report this repo's SQL as unnumbered** until `supabase/migrations/` has files in it: the tool keys on `NNNN_*.sql`, and everything under `supabase/*.sql` predates numbering. That is the correct reading of the tree, not a defect in the tool.

## Architecture (locked decisions)

- **Central members table is the hub.** Everything keys off the member ID: attendance, hours, profile, skills, and every future module. Never break this. New tables reference member ID.
- **One app, code-split routes.** Each feature is a lazy-loaded bundle that only downloads when opened. This keeps check-in fast no matter how many modules exist.
- **Check-in is the fast path.** The NFC tag deep-links straight to a deliberately minimal check-in route. Keep it light. Do not pull the dashboard, nav, or other modules into that route.
- **Public site stays separate** from team data. No login, different audience, different security.
- **Read and write RLS differ.** Students write only their own attendance; all members can read the team hours board. Respect this split when adding policies.
- **Parent vs staff role resolution.** `is_staff()` is mentor/lead/admin only — `parent` is never staff. A parent who ALSO holds a staff role is treated as staff and sees the mentor view. Any parent-specific UI must gate on `(hasRole('parent') && !isStaff)`, never on `hasRole('parent')` alone.

## Database, and how SQL actually reaches it

**By hand, in the Supabase SQL editor, and nothing in this repository records which files have been run.** Every rule below follows from that one fact, and it is the fact most likely to be assumed away.

- There is **no** `supabase_migrations.schema_migrations` history table in play, no `supabase db push` in any workflow, and no CI step that touches the live project. **CI reads no credential at all.**
- So **a file existing on `main` says nothing about the database having it.** The only proof a migration is live is querying the live project for the objects it creates — the table, the index by name, the constraint by name, the function in `pg_proc` — and reporting what was found. Not that a file exists. Not that a report said "applied".
- **58 SQL files, none numbered**: 57 under `supabase/`, one under `sql/`. 56 declare their own apply instruction in a header comment, some form of *"Run once in the Supabase SQL editor, BEFORE testing the UI"*. That line is the apply instruction and it is addressed to a person.
- **Three have a `_rls_test.sql` sibling** — `feedback`, `member_applications`, `parent_responses` — each running inside a `begin … rollback` so it is safe against live data, and each carrying positive controls so a broken-shut policy cannot fake a pass. That is the proof pattern where one exists.
- **From 2026-09-02 forward, a new SQL file is a numbered migration under [`supabase/migrations/`](supabase/migrations/), with its test beside it.** Read that directory's README before writing one: it carries the numbering rule, the frozen-foundation rule (nothing under `supabase/*.sql` or `sql/` is ever renumbered or moved), and the conventions learned from files that had to be pasted twice.
- **Take the next migration number by reading that directory, and record it in your prompt-ledger entry before starting.** Two parallel sessions will otherwise both read the same highest number and both pick the next one.
- **Edge Functions deploy separately, by hand.** `supabase/config.toml` pins `verify_jwt = false` for the five that are invoked by pg_cron or by capability URL. `discord-calendar` is the one function that is not single-file and so cannot be deployed from the Dashboard editor.

### The traps that make a wrong migration silent

- **A missing policy fails at 0 rows, not with an error.** The project's bootstrap `ALTER DEFAULT PRIVILEGES` grants `authenticated` everything on a new public table, so where an operation must be impossible, `revoke` it explicitly. Discovered on `feedback.sql`, where a delete block was silent until the revoke was added.
- **A column revoke is a no-op while a table-level grant exists.** Effective column privileges are the UNION of the column ACL and the table ACL. Hiding a column means dropping the table-level grant and re-granting every other column individually — `parent_responses.sql` does this for `parent_token` and explains it. `calendar_token.sql` uses the naive form and is flagged as probably not blocking anything.
- **An UPDATE has to FIND its rows, and row lookup goes through the SELECT policy.** So a test that thinks it is checking UPDATE is often checking SELECT again, and a wide-open UPDATE policy can pass a naive test. This cost a real miss on `feedback_rls_test.sql`: seven negative controls were run and the first version caught only six.
- **`ON DELETE CASCADE` is not blocked by a revoke.** Measured against a real PostgreSQL 16: a cascade runs as the referencing table's owner with row security not forced, so the caller's grants and RLS are not consulted for the rows it removes. Do not "fix" a working cascade by granting delete on the child tables — that hands every member a direct delete on other people's rows.
- **SQL dollar-quoting**: use named tags like `$fn$ … $fn$` for function bodies, never bare `$$`. Bare dollar signs break on paste.
- **`profiles` has no `email` column.** Email lives on `auth.users`; `admin_get_members()` joins it via SECURITY DEFINER. A client `from('profiles').select('…, email, …')` errors and — because callers ignore the error and `?? []` — silently yields an empty list. That is what had blanked the skill-coverage matrix. Use `full_name`/`nickname` for display, or the admin RPC for email.

## Verification standard

This applies to every change. Prompts do not need to restate it.

**Always:**

- **`npm run build` is green**, and so are `npm test`, `npm run ds:audit`, `npm run discord:calendar:test` and `npm run history:verify`. Those five are what CI runs. A session reports the numbers, not the adjective.
- **State plainly what was NOT verified, and why.** Nothing in this container can reach the live Supabase project — no credentials exist here — so any claim about RLS behaving on the real database, about a migration having been applied, or about a signed-in surface, is unverified and says so. **"Not verified" is a result; silence is not.**
- **A measurement that cannot be repeated is a claim about the past.** For months every bundle here wrote a headless Chromium harness, drove the real components against a stubbed `./supabase`, reported numbers, and then DELETED the harness. Those numbers are in `docs/history/` — 78/78, 54 checks, 62/62, 44/44 — and not one can be re-run. **Do not do this again.** A harness worth writing is worth keeping: add a route spec under `tools/browser-verify/routes/`, or a test under `tests/`.
- **Every test and every check needs a positive control.** An assertion that something is absent, empty, refused or zero is satisfied by a module that has stopped working entirely. Drive the same fixture the other way round in the same file. This is not a style preference: `tests/anomaly-pairing.test.js` mutates the shipped pairing function in memory and requires three ledgers to break, because without that every assertion in it would pass against a function returning an empty set.
- **Assert both directions of any visibility or gating claim.** Name what must be PRESENT alongside what must be ABSENT, and report both counts. "The parent view has no staff controls" is not a result; "0 of 3 staff links, against 3 on the same fixture with `isStaff` true" is.

**The instruments, and what each can and cannot see:**

| | what it proves | what it cannot see |
| --- | --- | --- |
| `npm test` (vitest, `tests/`) | pure logic: hours math, anomaly pairing, the Discord engine, vocabulary drift between `src/` and the SQL files | anything rendered; anything about the live database |
| `npm run verify:browser` | real geometry, contrast, presence and console errors in a real Chromium, on `/_ds` | every authenticated route — see below |
| `npm run ds:audit` | the design system's source invariants, 30 numbered checks | what a generated deck added |
| `_rls_test.sql` siblings | an RLS boundary, run by hand in the SQL editor | anything, until somebody runs it |

**The browser pass drives ONE route, and that is the honest state of this repo.** `/_ds` is the only route that renders without a real Supabase session. Everything else sits behind a real Google or OTP sign-in against the live project; there is no `/dev` route family here and no committed fixture-mounting harness. Growing that list means building one first. `tools/browser-verify/README.md` says so and names it as the next step.

**`ds:audit` and the design standards' pre-delivery checks number INDEPENDENTLY and must never be cross-referenced by number.** Always name which: "pre-delivery check 43", "`ds:audit` check 16". Both run, because source cannot see what a deck added and a deck cannot see whether the rule it relied on ships.

**Mutation proof is required where a wrong result is invisible in normal use** — RLS boundaries, exclusion filters, data-visibility rules, any assertion that a row does NOT appear. Mutate in the PERMISSIVE direction: a policy commented out entirely fails closed and reddens almost nothing, while `using (true)` reproduces the real leak. **Restore from a copy you made, never with `git checkout --`** — that restores from HEAD and silently discards the session's own uncommitted work, after which the remaining mutants test a tree that no longer has the change in it and all pass. **The tell is a mutation suite that suddenly all passes.**

## Working conventions

1. **Surgical edits only.** Edit the smallest unique chunk with a targeted replace. Never rewrite a whole file when a targeted edit works. Never re-output a full file after editing.
2. **Schema and UI ship together.** When a feature needs a column or table, put the SQL and the UI change in the same task so they never drift. Column-mismatch bugs come from shipping one without the other.
3. **Run the SQL in Supabase before testing.** A feature that reads a new column throws "column does not exist" until it is applied. Apply it first, by hand, in the SQL editor.
4. **Supabase holds live team data.** Never run destructive schema or data operations without explicit confirmation. Treat migrations and table changes as high-risk and propose first.
5. **Do not commit secrets.** The Supabase URL and anon key live in env vars and Vercel settings; the service role key never appears in client code. `.env.dsspec` is TRACKED and is the exception that proves the rule: it holds placeholders pointing at a port nothing listens on, so `/_ds` boots from a clean checkout.
6. **Commit and push in the same task.** Do not leave changes unstaged. Push success is the confirmation signal.
7. **Do not duplicate a rule.** A second implementation of a check, a formatter, or a piece of arithmetic is the thing that quietly stops matching. When two places need one behaviour, extract it — `src/hoursResolve.js` and `src/categories.js` exist for exactly this. Where a duplicate is unavoidable, say so at both copies and gate both in CI.
8. **A session that creates SQL files lists their full repo paths at the end of its response**, one line per file, in apply order. They are pasted into the SQL editor immediately, so the path belongs where it is about to be used. A session that created none says nothing.
9. **Bundle multiple changes into one task** rather than one item per task, and build in milestones.
10. **The system is in active pilot.** Favor changes safe to ship to live users, and flag anything that could disrupt an in-progress check-in session.

### Branches, and what happens to them

- **Work on the `claude/**` branch the harness minted for the session. Push it. Do not merge to `main`.**
- **A finished `claude/**` branch vanishing on its own is CORRECT, not a mistake.** `.github/workflows/integrate.yml` merges any `claude/**` branch into the long-lived `integration` branch the moment CI goes green on its tip, and deletes it. The commits are on `integration`; nothing is lost.
- **`main` moves only when a person moves it.** No workflow in this repo pushes to `main` except `deploy.yml`, which requires a typed confirmation. Every push to `main` deploys the PWA to students who may be mid-check-in, and this repo's SQL is applied by hand with nothing recording what has been applied — so a machine merging on green CI would ship code calling a table the database does not have.
- **A `claude/**` branch still standing after a session ends is a SIGNAL, not a leftover**: its CI failed, its CI has not finished, or its merge into `integration` conflicted. Name it in the report.
- **Never force-push.** Not `--force`, not `-f`, not `--force-with-lease`.
- **If a session ends with a branch still open, report the branch name and what is unfinished on it.** An unmerged branch is invisible work.

### How tasks come in

Default: tasks are described by intent and constraints. You own the implementation. Read the repo, find the right place, and write the code yourself. That is the normal mode.

Exact content is provided only in two cases:

- A file or snippet was drafted in chat and must land verbatim. Use it as given.
- A change is precise enough that an exact locator prevents the wrong edit. These arrive as CURRENT / REPLACE blocks: read the actual file first, apply even if the anchor text differs slightly, and report any miss instead of guessing.

Either way, read the repo file yourself rather than requiring a full-file paste to resync.

**Every claim in a prompt about the current state of the repo is a claim, not a fact.** Confirm each against the tree before building on it. Where the tree disagrees, the tree wins and the report says which claims were wrong.

## Known traps

- **PWA cache.** The service worker serves stale copies after a deploy. Test in an incognito window to see the true current version. A "missing build" is almost always cache, and it is the first thing to check rather than the last.
- **Web NFC is Chrome/Android only.** Keep that constraint in mind for any check-in change.
- **The check-in fast paths are outside `ProtectedLayout`** and must stay light. `/checkin` and `/checkin-volunteer` deliberately import none of the dashboard, the nav, the feedback widget or the notification code. The NFC tag deep-links straight there.
- **`sessionsFromEvents` overwrites an open IN when a second IN arrives**, so a `double_in` orphan produces no session row at all. That is why `unclosedInIds` exists in `MemberHoursAdmin.jsx` and why it cannot be replaced by reading sessions. `tests/anomaly-pairing.test.js` pins both halves.
- **`innerText` applies `text-transform`; `textContent` does not.** A copy check that compares against `innerText` reports strings missing from nodes that plainly contain them, because CSS uppercased them. Measured on `/_ds`, whose header is `v1.1.0 · /_ds · dev only` in source and `V1.1.0 · /_DS · DEV ONLY` rendered.
- **`opacity` is not inherited.** A child of an `opacity: 0` parent computes opacity 1, so any visibility check that does not walk ancestors reports elements visible while they are painted nowhere.
- **`chromium.executablePath()` points at a build this container does not have.** playwright-core 1.62.1 wants build 1234; the image ships 1194, under a differently-named directory. The resolution chain in `tools/browser-verify/browser.mjs` is load-bearing, and upgrading playwright-core would make it worse, not better — the download it would then want is not reachable.
- **The two Discord renderers disagree about subteams ON PURPOSE and are not being reconciled.** `discord-calendar/lib/render.js` matches a curated 11-alias map over title+notes and emits Discord ROLE names, because its output pings a role and only an existing role can be pinged. `api/_lib/weekAhead.js` matches the canonical 12-value `src/subteams.js` vocabulary over the TITLE ONLY and emits app subteam names, because its output is text a student reads. The scope difference follows: the canonical vocabulary contains `Management`, `Media`, `CAD` and `Fabrication`, and running those over notes prose ("Electrical runs wire management") attributed a build session to Management, which is false. **Do not try to make the two lists match.**
- **The event-kind vocabulary is duplicated across four sites with nothing enforcing agreement** (`SchedulePage.jsx` `KINDS`, the `events_kind_check` constraint, and `KIND_COLORS`/`KIND_LABELS` in `discord-calendar/lib/render.js`). Adding a kind means touching all of them. The subteam and hour-category vocabularies ARE enforced, by `tests/vocabulary-drift.test.js`; this one is not, because two of its four sites are not machine-comparable from here.

## Self-Maintenance

Keep this file accurate. When you discover during a task that any of the following has drifted from what is written here, update it in the same commit as your other changes:

- Repo structure, key paths, or file names changed.
- The stack, Supabase schema, or external integrations changed.
- A module shipped (move it from Roadmap to The subsystems) or a new milestone landed.
- An architecture decision or standing rule was established or revised.

**The historical record does NOT go here any more.** Write `docs/history/<your branch slug>.md` instead — one file per bundle, front matter then prose. That directory's README carries the format. This file describes the repo as it is now; the record describes how it got there, and mixing the two is what produced a 109,839-character line that every session had to rewrite.

**If a task raises a question only Mr. Pina can answer, write `docs/decisions/NN-slug.md` with a stated default and proceed under it.** Do not record it as a finding in a paragraph and carry it forward: three questions sat in the old "Last reviewed" line for weeks that way, phrased as "recorded and deliberately not fixed".

When you update this file, bump the Last reviewed date below and note what changed in the commit message.

Last reviewed: 2026-09-02 — repo brought into conformance with the workflow standard's section 2: this file reorganised into seven sections with no rule dropped, its 109,839-character history paragraph split into `docs/history/` (50 entries, verified lossless), and CI, a test suite, a standing browser harness, a prompt ledger, a decisions directory and `supabase/migrations/` established. See `docs/history/repo-standards-conformance-uk5er7.md`.
